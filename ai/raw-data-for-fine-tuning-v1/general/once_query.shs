{
  prompt: """How to execute something only once?""" 
  
  solution:
  """Once({
  Msg("This will only execute once")
  })"""
  
  explicit:
  """Once(Action:{
  Msg(Message: "This will only execute once" Level: LogLevel::Info)
  } Every: 0.0)"""
  
  explanation: """When the Once Shard has 0.0 in the Every parameter, it will execute what is stated in the Action parameter only once per flow execution.
  """
  
  tag: "  #General"
}