name: Build (Zig Cross-Compile)

on:
  push:
    branches:
      - devel
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:
    inputs:
      rust-cache:
        description: Use existing rust cache?
        required: false
        default: false
        type: boolean
      target:
        description: 'Target triple'
        required: true
        default: 'aarch64-linux-musl'
        type: choice
        options:
          - aarch64-linux-musl
          - x86_64-linux-musl
          - riscv64-linux-musl
          - riscv32-linux-musl

concurrency:
  # A new push to a PR cancels that PR's in-progress run; pushes to devel run to completion.
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build-cross:
    strategy:
      fail-fast: false
      matrix:
        target:
          - aarch64-linux-musl
          - x86_64-linux-musl
          - riscv64-linux-musl
          - riscv32-linux-musl

    name: Build (${{ matrix.target }})
    runs-on: ubuntu-22.04

    steps:
      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: false

      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: 0.15.2

      - name: Install dependencies
        run: |
          sudo apt-get -y update
          sudo apt-get -y install build-essential cmake ninja-build llvm libclang-dev

      - name: Setup Rust
        run: |
          RUSTUP_TOOLCHAIN=$(cat rust.version)
          echo "RUSTUP_TOOLCHAIN=$RUSTUP_TOOLCHAIN" >> $GITHUB_ENV
          rustup toolchain install $RUSTUP_TOOLCHAIN
          rustup component add rust-src --toolchain $RUSTUP_TOOLCHAIN

      - name: Add Rust target
        run: |
          case "${{ matrix.target }}" in
            aarch64-linux-musl)
              rustup +$RUSTUP_TOOLCHAIN target add aarch64-unknown-linux-musl
              ;;
            x86_64-linux-musl)
              rustup +$RUSTUP_TOOLCHAIN target add x86_64-unknown-linux-musl
              ;;
            riscv64-linux-musl)
              # No pre-built std for riscv64gc-unknown-linux-musl, uses -Zbuild-std
              echo "riscv64gc-unknown-linux-musl uses -Zbuild-std"
              ;;
            riscv32-linux-musl)
              # Tier 3 target - uses -Zbuild-std, no rustup target needed
              echo "riscv32gc-unknown-linux-musl is a Tier 3 target, using -Zbuild-std"
              ;;
          esac

      - name: Configure
        run: |
          cmake -Bbuild/Zig-${{ matrix.target }} -GNinja \
            -DCMAKE_TOOLCHAIN_FILE=cmake/Zig.cmake \
            -DZIG_TARGET=${{ matrix.target }} \
            -DCMAKE_BUILD_TYPE=Release \
            -DRUST_BUILD_TYPE=Small \
            -DSHARDS_WITH_EVERYTHING=OFF \
            -DSHARDS_WITH_LANGFFI=ON \
            -DSHARDS_WITH_ASSERT=ON \
            -DSHARDS_WITH_BROTLI=ON \
            -DSHARDS_WITH_CHANNELS=ON \
            -DSHARDS_WITH_CORE=ON \
            -DSHARDS_WITH_DEBUG=ON \
            -DSHARDS_WITH_FS=ON \
            -DSHARDS_WITH_HTTP=ON \
            -DSHARDS_WITH_JSON=ON \
            -DSHARDS_WITH_NETWORK=ON \
            -DSHARDS_WITH_OS=ON \
            -DSHARDS_WITH_RANDOM=ON \
            -DSHARDS_WITH_SNAPPY=ON \
            -DSHARDS_WITH_SQLITE=ON \
            -DSHARDS_WITH_TRACY=ON \
            -DSHARDS_WITH_MARKDOWN=ON \
            -DSHARDS_WITH_STRUCT=ON \
            -DSHARDS_WITH_REFLECTION=ON \
            -DSHARDS_WITH_BIGINT=ON \
            -DSHARDS_WITH_CSV=ON \
            -DSHARDS_WITH_RUN=ON

      - name: Build
        run: |
          cmake --build build/Zig-${{ matrix.target }} --target shards

      - name: Strip binary
        run: |
          llvm-strip build/Zig-${{ matrix.target }}/shards
          ls -lh build/Zig-${{ matrix.target }}/shards
          file build/Zig-${{ matrix.target }}/shards

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: shards-${{ matrix.target }}
          path: build/Zig-${{ matrix.target }}/shards
          if-no-files-found: error
          retention-days: 7
