A step that can be passed to [Render](../Render). When processed it will take all Drawables from the referenced [DrawQueue](../DrawQueue) and draw them.

Features specified on the DrawablePass will be applied to all objects that are drawn as part of that pass.
