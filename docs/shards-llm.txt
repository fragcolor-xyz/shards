# Shards — LLM context pack

The canonical, always-loaded context for writing Shards. It is intentionally compact:
the language guide plus a one-line index of every shard. Expand any shard on demand
with the CLI instead of carrying full signatures here.

Agent loop:
  1. Discover    — `shards search <query>` or `shards enumerate --filter <substr>`
  2. Drill down  — `shards docs <name> --json`   (full params, types, defaults; ground truth)
  3. Write the .shs
  4. Verify      — `shards check <file> --json`   (parse + compose, never runs; structured errors)

The "Shard index" below is generated from the binary's introspection. It is a snapshot;
the live `enumerate`/`docs --json` commands are authoritative (the shard set varies by
platform/build).

## Language guide

# Shards Programming Language: Comprehensive Guide

## Core Concept: Data Flow Programming

Shards is a data flow programming language where data flows through operations using the pipe `|` operator. Think of it as a river system rather than containers:

> You might already know an old version of the syntax that was inspired by LISP but actually now Shards uses a completely different CUSTOM syntax!

```shards
// Traditional imperative thinking (WRONG):
x = 5          ❌ Not a container being filled
x += 1         ❌ Not incrementing a stored value

// Shards data flow thinking (CORRECT):
5 >= x         ✅ Creates a variable named 'x' and channels 5 into it
x | Add(1) > x   ✅ Takes flow from x, adds 1, and channels it back into x
```

## Basic Syntax

- **Comments**: Begin with `//` (C-style); `/* ... */` for block comments
- **Operations**: Chain with pipe `|`
- **No semicolons** at line ends
- **No braces** for basic code blocks
- **Whitespace** is flexible

## Variables and Data Flow

### Variable Assignment

```shards
// Immutable assignment (=)
5 = x                       // Immutable integer
"Hello" = greeting          // Immutable string
@f3(1.0 2.0 3.0) = position  // Immutable vector

// Mutable assignment (>=)
10 >= counter               // Mutable integer
[1 2 3] >= numbers          // Mutable sequence
{name: "Alice" age: 30} >= person  // Mutable table
```

### Updating Variables

```shards
// Update mutable variables (>)
counter | Add(5) > counter     // Add 5 to counter
4 >> numbers                 // Append to sequence (shorthand)
"Bob" | Update(person "name")  // Update table field
```

## Data Types

### Basic Types

```shards
42                // Integer
3.14              // Float
"Hello"           // String
true, false       // Boolean
none              // Null/None
0x1A3F            // Hexadecimal
```

### Vector Types

```shards
@f2(1.0 2.0)               // 2D float vector
@f3(1.0 2.0 3.0)           // 3D float vector
@f4(1.0 2.0 3.0 4.0)       // 4D float vector
@i2(1 2), @i3(1 2 3), @i4(1 2 3 4)  // Integer vectors
```

### Sequences

```shards
[1 2 3 4]         // Sequence of integers
["a" "b" "c"]     // Sequence of strings
[1 "mixed" true]  // Mixed types (valid)

// Accessing elements
sequence | Take(2)       // Get element at index 2
sequence | RTake(0)      // Get last element
sequence | Slice(1 3)    // Get elements from index 1 to 3 (exclusive)
sequence | Take([0 2 4]) // Get elements at indices 0, 2, and 4

// Modifying
value >> sequence        // Append to sequence
Erase(2 sequence)        // Remove element at index 2
Erase([0 1] sequence)    // Remove elements at multiple indices
[0 "new-value"] | Assoc(sequence)  // Update or create a field in a table using Assoc and a sequence of index to insert at and value
"new-value" | Insert(0 sequence)  // Insert a value at a specific index
```

### Tables

```shards
{name: "Alice" age: 30 city: "NY" optional: none} >= person  // Table creation

// or in full JSON compatible format
{
  "name": "Alice",
  "age": 30,
  "city": "NY",
  "optional": null
} >= person

// Accessing
person:name              // Access via colon sugar syntax, notice this is valid ONLY when reading values from a table and the key is a string (NOT A VARIABLE)
person | Take("age")     // Access via Take

// Modifying
"Bob" | Update(person "name")   // Update field
Erase("city" person)            // Remove field
Erase(["name" "age"] person)    // Remove multiple fields
["name" "Bob"] | Assoc(person)  // Update or create a field in a table using Assoc and a sequence of key and value
```

## String Operations

```shards
// String.Join - combine string sequences
["Hello" "World"] | String.Join            // "HelloWorld"
["Hello" " " "World"] | String.Join      // "Hello World"

// String.Format - mix strings with other types
["Score: " 42] | String.Format           // "Score: 42"
["Pos: " @f3(1 2 3)] | String.Format     // "Pos: @f3(1 2 3)"
```

## Control Flow

### If / When

```shards
// If with then/else
value | If({IsMore(10)}
  {"Greater than 10" | Log}
  {"Less or equal to 10" | Log}
)

// When (if without else)
value | When({IsMore(10)} {
  "Greater than 10" | Log
})
```

### Loops

```shards
// Repeat a specified number of times
Repeat({
  "Repeated action" | Log
} Times: 3)

// ForEach on sequence elements
[1 2 3] | ForEach({
  Mul(2) | Log
})
```

### Pattern Matching

```shards
value | Match([
  "A" {"Matched A" | Log}
  "B" {"Matched B" | Log}
  none {"No match" | Log}  // Default case
])

// or with passthrough off, we flow the output of the match to the next operation
value | Match([
  "A" {"Matched A"} // value & action shards
  "B" {"Matched B"} // value & action shards
  none {"No match"}  // Default case
] Passthrough: false) | Log

// using the even more flexible Cond shard
value | Cond([
  {Is("A")} {"Matched A" | Log} // condition shards & action shards
  {Is("B")} {"Matched B" | Log} // condition shards & action shards
  {true} {"No match" | Log}  // Default case
])
```

### Initialization

```shards
// One-time initialization block
Once({
  0 >= counter
  "" >= message
})
```

## Flow Behavior in Blocks

In Shards, understanding how data flows through blocks is essential:

### Sequential vs. Parallel Flow

```shards
// Sequential flow - each operation receives previous result
5 | {
  Add(1)     // Result: 6
  Mul(2)     // Result: 12 (6×2)
  IsMore(10) // Result: true (12>10)
} | Log      // Outputs: true

// Parallel flow - each sub-block receives original input
5 | {
  {Add(1) | Log}     // Outputs: 6
  {Mul(2) | Log}     // Outputs: 10 (5×2)
  {IsMore(3) | Log}  // Outputs: true (5>3)
}
```

### The Universal Standard

In Shards, data flow is the fundamental principle:

1. Input flows through the pipe operator `|` to operations
2. Operations receive their input from the flow
3. Nested blocks within ANY construct receive input from their parent flow
4. NO explicit references (like $0) are strictly needed - the value flows naturally

This is the standard behavior across the language and applies to virtually all shards and constructs (Cond, If, Match, etc.), though specific shards may have their own flow behavior (like ForRange providing the index).

### Examples of Universal Flow

```shards
// In condition blocks
number | When({IsMore(10)} {"Big" | Log})  // IsMore receives 'number'

// In match patterns
value | Match([
  "A" {"Matched A" | Log}  // Each action receives 'value'
  none {Log}  // Logs the original value
])

// In loops
[1 2 3] | ForEach({
  Mul(2) | Log  // Each element flows directly to Mul
})
```

Remember: In Shards, explicit references are less needed - the value flows naturally through the system. This is the standard behavior unless documentation specifies otherwise.

## Sub Blocks for Multiple Operations

```shards
// When multiple operations need to process the same input
[1 2 3] | ForEach({
  {Mul(2) | Log}  // Each gets original input
  {Add(5) | Log}  // Processes same input independently
})
```

## Operation Grouping with Parentheses

```shards
// Without grouping: (5+3)*2 = 16
5 | Add(3) | Mul(2)

// With grouping: 5+(3*2) = 11
5 | Add((3 | Mul(2)))

// Nested grouping: 10+(5*(2+1)) = 25
10 | Add((5 | Mul((2 | Add(1)))))

// Within seqs, it will be applied to each element
5 |[
  (Add(1)) // 6
  (Mul(2)) // 12
] = my-seq

// Within tables, it will be applied to each element
5 | {
  a: [
    (Add(1)) // 6
    (Mul(2)) // 12
  ]
} = my-table
```

## Error Handling

```shards
// Maybe block for error handling (try-catch equivalent)
Maybe({
  // Try block - code that might cause error
  "42" | FromJson | Log
} {
  // Catch block - runs only if error occurs
  "Error occurred" | Log
})
```

## Templates for Code Reuse

```shards
// Define a template with parameters
@template(greet [name] {
  ["Hello, " name "!"] | String.Format
})

// Use the template, notice template calls are always prefixed with @ otherwise, Shards will interpret the name as a variable
@greet("Alice") | Log  // Outputs: "Hello, Alice!"

// Template with multiple parameters
@template(math-op [a b] {
  a | Add(b)
})

@math-op(5 10) | Log  // Outputs: 15

// Or using shards natural flow syntax
@template(math-op [b] {
  Add(b)
})

5 | @math-op(10) | Log  // Outputs: 15
```

## Wire Definitions and State Management

```shards
// Define a wire with its own state
@wire(main-wire {
  0 >= counter
  "Initial" = message
  
  counter | Log
  counter | Add(1) > counter
  counter | Log
})
```

### Child Wires and Variable Scope

```shards
@wire(parent {
  0 >= counter
  
  // Child with direct access to parent variables
  Do(child)
  counter | Log  // Shows child's modifications
  
  // Detached child with copies of variables
  Detach(independent-child)
  counter | Log  // Unchanged by independent child
})

@wire(child {
  // Modifies parent's counter directly
  counter | Add(5) > counter
})

@wire(independent-child {
  // Modifies only its local copy
  counter | Add(10) > counter
})
```

### Global Variables

```shards
// Create global variables accessible from any wire
0 | Set(global-counter Global: true)
"shared" | Set(global-message Global: true)
"value" | Set(global-table "key" Global: true)

// Access globals from any wire
@wire(some-wire {
  global-counter | Add(1) > global-counter
  global-message | Log
})
```

## Parameter Rules

```shards
// Basic unnamed parameters
Add(5)

// Once you use a named parameter, all subsequent must be named
Http.Get(
  URL: "https://api.example.com"
  Headers: headers
  Timeout: 30
)

// Error - mixing unnamed after named
Http.Get(URL: "https://api.example.com" headers)  // Error!
```

## Concurrency

```shards
// Detach for non-blocking execution
@wire(background-task {
  Pause(5.0)  // Simulates long operation
  "Task complete" | Log
})

@wire(main {
  "Main running" | Log
  Detach(background-task)  // Non-blocking
  "Continued immediately" | Log
})
```

## Common Patterns and Practices

1. **Think in flows, not containers**
2. **Use meaningful variable names in kebab-case**
3. **Shards are Capitalized, variables are lowercase**
4. **Initialize all global variables in a single wire**
5. **Use `Do(wire)` for shared state, `Detach(wire)` for isolation**
6. **Use Tables for structured data**
7. **Use Maybe for robust error handling**
8. **Use templates for reusable code patterns**

## Program Structure Example

```shards
// Wire definitions
@wire(initialization {
  0 | Set(global-counter Global: true)
  // Initialize other globals...
})

@wire(main-logic {
  // Application logic here
  Do(sub-process)
})

@wire(sub-process {
  // Handle part of the logic
})

// Start the program
Do(initialization)
Do(main-logic)
```

Remember: In Shards, variables are not containers holding values but channels directing data flow. The pipe `|` operator shows how data flows through your system.

## Shard index

```
AI.AddAudio                      Seq → Seq
AI.AddImage                      Image → Image
AI.AddText                       String → String
AI.Chat                          Object → Object
AI.Detokenize                    Seq → String
AI.Embed                         String/Seq → Seq
AI.Generate                      Object → String
AI.Model                         String → Object
AI.Reset                         Object → Object
AI.Tokenize                      String → Seq
Abs                              Int/Int2/… → Int/Int2/…  This shard outputs the absolute value of the input.
Acos                             Float/Float2/… → Float/Float2/…
Add                              Int/Int2/… → Int/Int2/…  This shard adds the input value to the value provided in the Operand parameter.
And                              Bool → Bool  If the input of the preceding shard is true, the flow continues; otherwise, the flow stops. This shard is typically used within conditional flows (e.g., If, When) to chain conditions. Note: Outside a conditional flow, it might restart the current wire, which can be used as a trick in certain scenarios.
Animation.Duration               Seq → Float  Outputs the total duration of the animation in seconds.
Animation.Interpolated           Float/Float2/… → Float/Float2/…  Whenever the input value is changed, this shard will interpolate between the old value and the new value over the duration of the animation and output the result.
Animation.Play                   Float → Seq  Evaluates and interpolates the animation of the glTF model specified in the Animation parameter. The output of this shard is applied to the glTF model to play animations.
Animation.Timer                  None → Float  This shard sets a duration (either taken form the animation object in the Animation Parameter or specified in the Duration parameter) and executes the shards specified in the Action parameter whenever that duration is reached.
AppendTo                         Any → Any
Argon2id.Hash                    String → String  Hashes a password using the Argon2id algorithm.
Argon2id.Verify                  String → Bool  Verifies a password against an Argon2id hash.
Asin                             Float/Float2/… → Float/Float2/…
Assert.ComposeFailure            Any → Any
Assert.ComposeSuccess            Any → Any
Assert.Is                        Any → Any  This assertion is used to check whether the input is equal to a given value.
Assert.IsAlmost                  Float/Float2/… → Any  This assertion is used to check whether the input is almost equal to a given value.
Assert.IsNot                     Any → Any  This assertion is used to check whether the input is different from a given value.
Assert.IsStatic                  None → Any  Asserts that a value is static at run-time
Assert.IsVariable                None → Any  Asserts that a value is a variable at run-time
Assoc                            Seq → Seq
Atan                             Float/Float2/… → Float/Float2/…
Atan2                            Int/Int2/… → Int/Int2/…
Audio.Channel                    Any → Any  This shard represents an audio channel in the mesh. It manages the routing and processing of audio data between input and output buses, applies volume control, and executes custom audio processing shards. Audio.Channel works in conjunction with Audio.Device to handle audio processing within the shards system.
Audio.Compress                   Audio → Seq
Audio.Compressor                 Audio → Audio
Audio.Cones                      Float3 → Float3  The Audio.Cones shard sets the sound cone properties for a 3D sound object. Sound cones are used to create directional audio effects, where the volume of the sound changes based on the angle between the sound's direction and the listener's position. It takes the Sound object, created by Audio.Sound that is specified in the Sound parameter, and sets its cone properties using the float3 vector(a vector with 3 float elements) provided as input. The first float value in the float3 vector represents the inner angle in radians, the second float value represents the outer angle in radians, and the third float value represents the outer gain. This shard is particularly useful for creating directional audio effects in 3D environments and is typically used in conjunction with Audio.Engine, Audio.Sound, Audio.Position, and Audio.Direction. Do note that the Spatialized parameter on Audio.Sound should be set to true when initializing a sound object meant for 3D audio.
Audio.Decompress                 Bytes → Audio
Audio.Device                     Any → Any  This shard initializes an audio device in the mesh. This device sets up the audio context, manages input and output channels, and handles the audio processing loop which is necessary for processing and playing audio in the shards system. In essence, the Audio.Device provides the underlying audio system, and can be used with other shards like Audio.ReadFile and Audio.Channel to process and play audio.
Audio.Direction                  Float3 → Float3  The Audio.Direction shard sets the direction of a sound object in 3D audio space. It takes the Sound object, created by Audio.Sound that is specified in the Sound parameter, and sets its direction to the 3D vector with x y z coordinates, represented as a float3 vector(a vector with 3 float elements), that is provided as input. The x coordinate represents its direction along the x-axis, the y coordinate represents its direction along the y-axis, and the z coordinate represents its direction along the z-axis. The float3 vector input should also be normalized so that it has a magnitude of 1. This shard is particularly useful for creating directional audio effects in 3D environments and is typically used in conjunction with Audio.Engine, Audio.Sound, and Audio.Position to manage 3D audio positioning and orientation. Do note that the Spatialized parameter on Audio.Sound should be set to true when initializing a sound object meant for 3D audio.
Audio.Engine                     Any → Any  This shard initializes an audio engine in the mesh, and this enables audio playback and processing capabilites. It manages resources, handles audio mixing, and provides spatial audio functionality. The Audio.Engine is used in conjunction with other audio shards like Audio.Sound, Audio.Play and Audio.Pause to create a complete audio system to process and play audio.
Audio.Envelope                   Audio → Audio
Audio.Filter                     Audio → Audio
Audio.MultiStageEnvelope         Audio → Audio
Audio.Noise                      None → Audio
Audio.Oscillator                 None/Audio → Audio
Audio.Pan                        Float → Float  The Audio.Pan shard adjusts the stereo panning of a sound object in the mesh, allowing for dynamic control over the spatial positioning of individual sound objects during playback. It takes the Sound object, created by Audio.Sound that is specified in the Sound parameter, and sets the pan position to the float value provided as input (-1.0 being full left and 1.0 being full right). It's typically used in conjunction with Audio.Engine, Audio.Sound, Audio.Start, and other audio shards to manage audio playback and create spatial audio effects.
Audio.Pause                      Object → Object  The Audio.Pause shard pauses playback of a sound object in the mesh. It takes a Sound object which was created by Audio.Sound and played by Audio.Start and pauses its playback. This shard is typically used in conjunction with Audio.Engine, Audio.Sound, Audio.Start, and Audio.Stop to manage audio playback and control.
Audio.Pitch                      Float → Float  The Audio.Pitch shard adjusts the pitch of a sound object in the mesh, thus allowing for dynamic control over the pitch of individual sound objects during playback. It takes the Sound object, created by Audio.Sound that is specified in the Sound parameter, and sets the pitch to the float value provided as input. 1.0 being the original pitch, values greater than 1.0 will increase the pitch, while values between 0 and 1.0 will decrease the pitch. 0.5, for example, will lower the pitch by one octave, while 2.0 will raise it by one octave. It's typically used in conjunction with Audio.Engine, Audio.Sound, Audio.Start, and other audio shards to manage audio playback and create pitch-based effects.
Audio.Position                   Float3 → Float  The Audio.Position shard sets the 3D position of a sound object in the audio space. It takes the Sound object, created by Audio.Sound, that is specified in the Sound parameter, and sets its position to the 3D coordinates represented as a float3 vector(a vector with 3 float elements) provided as input. This shard is particularly useful for creating spatial audio effects and is typically used in conjunction with Audio.Engine, Audio.Sound, and Audio.Direction to manage 3D audio positioning and orientation. Do note that the Spatialized parameter on Audio.Sound should be set to true when initializing a sound object meant for 3D audio.
Audio.ReadFile                   None → Audio  This shard reads audio data from a file or memory buffer. It supports various audio formats including wav, ogg, mp3, and flac. Audio.ReadFile is designed to be used in conjunction with Audio.Device and Audio.Channel to process and play audio in the shards system. It provides the audio data that can be further processed or played through the audio device.
Audio.ReadFileBytes              String → Bytes  This shard reads an entire audio file into bytes. Unlike Audio.ReadFile, this shard reads the complete file at once and outputs it as bytes, without the uint16_t sample count limitation of SHAudio. This is useful when you need to process large audio files in their entirety.
Audio.Remix                      Audio → Audio
Audio.Resample                   Audio → Audio  This shard resamples audio data.
Audio.Sound                      String → Object  The Audio.Sound shard initializes a sound object in the mesh. It loads an audio file and prepares it for playback. This shard is used in conjunction with other audio shards like Audio.Start, Audio.Pause, and Audio.Stop to control audio playback. It supports spatialization for 3D audio positioning and can be used with various audio effect shards for further processing. Do note that the Spatialized parameter on Audio.Sound should be set to true when initializing a sound object meant for 3D audio (if it is to be manipulated by Audio.Direction, Audio.Position, Audio.Velocity or Audio.Cones).
Audio.Start                      Object → Object  The Audio.Start shard begins playback of a sound object in the mesh. It takes a Sound object created by Audio.Sound and starts playing it and also allows control over whether the sound should loop or play once. It's typically used in conjunction with Audio.Engine, Audio.Sound, Audio.Pause, and Audio.Stop to manage audio playback.
Audio.Stop                       Object → Object  The Audio.Stop shard stops playback of a sound object in the mesh. It takes a Sound object which was created by Audio.Sound and played with Audio.Start and stops its playback, resetting the playback position to the beginning. This shard is typically used in conjunction with Audio.Engine, Audio.Sound, Audio.Start, and Audio.Pause to manage audio playback and control.
Audio.Velocity                   Float3 → Float3  The Audio.Velocity shard sets the velocity of a 3D sound object in the audio space. It takes the Sound object, created by Audio.Sound that is specified in the Sound parameter, and sets its velocity to the 3D vector, represented as a float3 vector(a vector with 3 float elements), provided as input. The first element in the float3 vector represents the velocity along the x-axis, the second element represents the velocity along the y-axis, and the third element represents the velocity along the z-axis. This shard is particularly useful for creating doppler effects and is typically used in conjunction with Audio.Engine, Audio.Sound, Audio.Position, and Audio.Direction to manage 3D audio positioning and movement. Do note that the Spatialized parameter on Audio.Sound should be set to true when initializing a sound object meant for 3D audio.
Audio.Volume                     Float → Float  The Audio.Volume shard adjusts the volume of a sound object in the mesh, thus allowing for the dynamic control over the volume of individual sound objects during playback. It takes the Sound object, created by Audio.Sound specified in the Sound parameter, and sets the volume to the float value provided as input. It's typically used in conjunction with Audio.Engine, Audio.Sound, Audio.Start, and other audio shards to manage audio playback and control.
Audio.WriteFile                  Audio → Audio  This shard writes audio data to WAV format file.
AudioToBytes                     Audio → Bytes
AudioToFloats                    Audio → Seq
Await                            Any → Any  Executes a shard or a sequence of shards asynchronously and awaits their completion.
BM25.Index                       Seq → Object
BM25.Query                       String → Seq/None
BigInt                           Int/Float/… → Bytes  Converts various input types to a big integer represented as bytes. Big integers are signed and can be of arbitrary size. This allows for users to work with numbers that are too large to be represented by standard integer types.
BigInt.Abs                       Bytes → Bytes  This shard computes the absolute value of a big integer.
BigInt.Add                       Bytes/Seq → Bytes/Seq  This shard adds the input big integer to the big integer specified in the Operand parameter and outputs the result.
BigInt.And                       Bytes/Seq → Bytes/Seq  This shard performs a bitwise AND operation on the input big integer with the big integer specified in the Operand parameter and outputs the result. A bitwise AND operation is a binary operation that compares each bit of the binary representations of two numbers and outputs 1 if the bits are 1 and 0 otherwise. The resulting number is a big integer representation of the 1s and 0s concatenated.
BigInt.Divide                    Bytes/Seq → Bytes/Seq  This shard divides the input big integer by the big integer specified in the Operand parameter and outputs the result.
BigInt.FromFloat                 Float → Bytes  This shard converts a floating point number to a big integer.
BigInt.Is                        Bytes → Bool  This shard checks if the input big integer is equal to the operand.
BigInt.IsLess                    Bytes → Bool  This shard checks if the input big integer is less than the operand.
BigInt.IsLessEqual               Bytes → Bool  This shard checks if the input big integer is less than or equal to the operand.
BigInt.IsMore                    Bytes → Bool  This shard checks if the input big integer is greater than the operand.
BigInt.IsMoreEqual               Bytes → Bool  This shard checks if the input big integer is greater than or equal to the operand.
BigInt.IsNot                     Bytes → Bool  This shard checks if the input big integer is not equal to the operand.
BigInt.Max                       Bytes/Seq → Bytes/Seq  This shard compares the input big integer with the big integer specified in the Operand parameter and outputs the larger of the two.
BigInt.Min                       Bytes/Seq → Bytes/Seq  This shard compares the input big integer with the big integer specified in the Operand parameter and outputs the smaller of the two.
BigInt.Mod                       Bytes/Seq → Bytes/Seq  This shard computes the modulus of the input big integer with respect to the big integer specified in the Operand parameter and outputs the result.
BigInt.Multiply                  Bytes/Seq → Bytes/Seq  This shard multiplies the input big integer with the big integer specified in the Operand parameter and outputs the result.
BigInt.Or                        Bytes/Seq → Bytes/Seq  This shard performs a bitwise OR operation on the input big integer with the big integer specified in the Operand parameter and outputs the result. A bitwise OR operation is a binary operation that compares each bit of the binary representations of two numbers and outputs 1 if the either or both bits are 1 and 0 if both bits are 0. The resulting number is a big integer representation of the 1s and 0s concatenated.
BigInt.Pow                       Bytes → Bytes  This shard raises the input big integer to the power of the exponent specified in the  Operand parameter.
BigInt.Shift                     Bytes → Bytes  This shard shifts the decimal point of the input big integer by the amount specified in the By parameter.
BigInt.Sqrt                      Bytes → Bytes  This shard computes the square root of the input big integer represented as bytes.
BigInt.Subtract                  Bytes/Seq → Bytes/Seq  This shard subtracts the input big integer from the big integer specified in the Operand parameter and outputs the result.
BigInt.ToBytes                   Bytes → Bytes  This shard converts a big integer value into a bytes representaion with the specified bit length set in the Bits parameter. If set to 0, the output will be the minimum number of bytes required to represent the big integer. If set to a positive number, the output will be padded with leading zeros to match the specified bit length. An error is thrown if the number requires more bits than specified.
BigInt.ToFloat                   Bytes → Float  This shard converts the input big integer value to a floating point number.
BigInt.ToHex                     Int/Bytes/String → String  This shard converts the input big integer value to its hexadecimal representation.
BigInt.ToInt                     Bytes → Int  This shard converts the input big integer value to an integer.
BigInt.ToString                  Bytes → String  Converts the value to a string representation.
BigInt.Xor                       Bytes/Seq → Bytes/Seq  This shard performs a bitwise XOR operation on the input big integer with the big integer specified in the Operand parameter and outputs the result. A bitwise XOR operation is a binary operation that compares each bit of the binary representations of two numbers and outputs 1 if the bits are different and 0 if they are the same. The resulting number is a big integer representation of the 1s and 0s concatenated.
BitSwap32                        Int → Int  This shard takes a 32-bit integer, reverses their order of its bytes, and outputs the result as an integer. This is useful for converting between different endianness formats.
BitSwap64                        Int → Int  This shard takes a 64-bit integer, reverses their order of its bytes, and outputs the result as an integer. This is useful for converting between different endianness formats.
Branch                           Any → Any  Creates a branch from the specified Behavior and schedules all the Wires specified. Every time this shard is called, it will progress the state of all the Wires specified asynchronously and continue execution of the current Wire. This shard is like a mass Step, where it Steps all the Wires specified.
Broadcast                        Any → Any
Brotli.Compress                  Bytes → Bytes  This shard compresses the input byte array using the Brotli algorithm and outputs the smaller compressed byte array. The size of the resulting byte array and the speed of compression can be controlled using the Quality parameter.
Brotli.Decompress                Bytes → Bytes  This shard decompresses the input byte array that has been previously compressed using the Brotli.Compress shard.
Browse                           String → String  This shard will open the URL string input in the current system's default web browser.
Bytes.Join                       Seq → Bytes  This shard will concatenate a sequence of strings or bytes into a single string or byte array and output it as a byte array.
BytesBuffer                      Int → Bytes
BytesToAudio                     Bytes → Audio
BytesToInts                      Bytes → Seq  Convert bytes into a sequence of integers. Each byte is interpreted as an integer and stored in the sequence.
BytesToString                    Bytes → String  Converts a sequence of bytes into a string. Each byte in the sequence is interpreted as a character in the resulting string.
CRDT.Apply                       Seq → Seq
CRDT.ChangesSince                Int → Seq
CRDT.CompactTombstones           Int → Int
CRDT.Delete                      Int16 → Table
CRDT.Get                         Int16 → Any
CRDT.New                         Any → Object
CRDT.Set                         Any/Seq → Table/Seq
CRDT.Tombstones                  Any → Int
CRDT.Version                     Object → Int
CSV.Read                         String → Seq  Reads a CSV string and outputs the data as a sequence of strings in a sequence of rows.
CSV.Write                        Seq → String  Reads a sequence of strings in a sequence of rows and outputs the data as a CSV string.
CaptureLog                       None → Seq
Ceil                             Float/Float2/… → Float/Float2/…  This shard rounds up the input to the nearest integer.
ChaChaPoly.Decrypt               Bytes → Bytes  This shard decrypts an input ChaCha20-Poly1305 encrypted byte array using the provided key (provided in the Key parameter). It then outputs the decrypted byte array.
ChaChaPoly.Encrypt               String/Bytes → Bytes  This shard takes the input string or byte array and encrypts it using the ChaCha20-Poly1305 algorithm with the provided key (provided in the Key parameter). It then outputs the encrypted byte array.
Clamp                            Int/Int2/… → Int/Int2/…  This shard ensures the input value falls within the specified range. If the value falls below the minimum, the Min value is returned. If the value exceeds the maximum, the Max value is returned. Otherwise, the value is returned unchanged.
Clear                            Any → Any  Clears all elements from the sequence or table passed to it. Applicable only to sequences and tables. For sequences, this operation is very fast as Shards recycles memory extensively. If the variable does not exist or the type is not a sequence or table, it simply passes through without failing.
Comment                          Any → Any  A comment shard that does nothing.
Complete                         Any → Any
Concat                           Seq → Seq
Cond                             Any → Any  Takes a sequence of conditions and predicates. Evaluates each condition one by one and if one matches, executes the associated action.
Const                            None → Any  Declares an un-named constant value (of any data type).
Consume                          Any → Any
Convolve                         Image → Image  This shard extracts a patch from an image of dimensions specified in the Radius parameter and outputs the extracted patch as an image. Everytime the shard is subsequently called, the centre of the patch to extract is advanced by the number of pixels specified in the Step parameter.
Cos                              Float/Float2/… → Float/Float2/…  This shard calculates the cosine of the given input, where the input is the angle in radians.
Count                            Any → Int
CurrentCaptureLog                None → Seq
DB.Backup                        Any → Any
DB.LoadExtension                 Any → Any
DB.Query                         Seq → Table/Seq
DB.RawQuery                      String → String
DB.Transaction                   Any → Any
DSP.FFT                          Seq/Audio → Seq  This shard performs a Fast Fourier Transform (FFT) on the input. It takes the time-domain representation of an audio (represented as a sounds object, float sequence or float2 sequence) and outputs its frequency-domain representation as a float2 sequence.
DSP.IFFT                         Seq → Seq/Audio  This shard performs an Inverse Fast Fourier Transform (IFFT) on the input. It takes the frequency-domain representation of an audio (represented as a float2 sequence) and outputs its time-domain representation (as a float sequence, float2 sequence or an audio object.)
Date.Format                      Int → String  Reads an epoch timestamps and formats it into a readable string.
Date.Parse                       String → Int
Debug.Break                      Any → Any
Debug.DumpEnv                    Any → Any  Dumps the variable environment during compose
Debug.LastError                  Any → Any  Outputs the last error
Debug.Noop                       Any → Any  A shard that can be set to break in the debugger at a specific location
Debug.WireStack                  Any → Any  Dumps the current wire stack during activation
Dec                              Any → Any  Decreases the input by 1.
DecCounter                       Any → Int
DegreesToRadians                 Float → Float  This shard converts the input angle from degrees to radians. The conversion is done using the formula: radians = degrees * (π / 180).
DemultiplyAlpha                  Image → Image  Applies the demultiplication of alpha channels of an image to its RGB channels. Does nothing if the image has already been demultiplied or never been premultiplied in Shards. This mainly applies to PNG images.
Detach                           Any → Any  Schedules and executes the specified Wire asynchronously. The current Wire will continue its execution independently of the specified Wire. Unlike Spawn, only one unique copy of the specified Wire can be scheduled using Detach. Future calls of Detach that schedules the same Wire will be ignored unless the specified Wire is Stopped or ends naturally.
Div                              Int/Int2/… → Int/Int2/…  This shard divides the input value by the value provided in the Operand parameter.
Do                               Any → Any  Schedules and executes the specified Wire inline of the current Wire. The specified Wire needs to complete its execution before the current Wire continues its execution. This means that a pause in execution of the child Wire will also pause the parent Wire.
DoMany                           Seq → Seq  This shard takes a sequence of values as input, schedules multiple copies of a specified Wire and executes them sequentially. Each value from the sequence is provided as input to its corresponding copy of the specified Wire. The shard then outputs a sequence of values containing the output of all copies of the specified Wire.
Drop                             Any → Any  Drops the last element of the sequence variable. Works only on sequences. If the variable is not a sequence, it simply passes through without failing.
DropFront                        Any → Any  Drops the first element of the sequence variable. Works only on sequences. If the variable is not a sequence, it simply passes through without failing.
ECDSA.PublicKey                  Bytes → Bytes  Generates the public key from the private key using the ECDSA algorithm.
ECDSA.Recover                    Bytes → Bytes  Recovers the public key from the signature and message using the ECDSA algorithm.
ECDSA.Seed                       String → Bytes  Generates the private key from the seed using the ECDSA algorithm.
ECDSA.Sign                       Bytes → Bytes  Signs a message with the private key using the ECDSA algorithm.
Ed25519.PublicKey                Bytes → Bytes  Extracts the public key from an Ed25519 signing key
Ed25519.Sign                     Bytes → Bytes  Signs a message using the Ed25519 signature algorithm.
Ed25519.Verify                   Bytes → Bool  Verifies an Ed25519 signature
Entries                          Table → Seq
Env                              String → String  Get environment variables.
Env.All                          None → Seq
Erase                            Any → Any
Events.Check                     None → Bool
Events.Emit                      Any → Any
Events.Receive                   None → Seq
Events.Send                      Any → Any
Events.Update                    Any → Any
Exit!                            Int → None
Expand                           Any → Seq  Schedules (n) number of copies of the specified Wire, where (n) is the number specified in the Size parameter. The parent Wire will wait until all the scheduled copies have ended and will either return a sequence of values outputs of all the copied Wires or the output of the first Wire that succeeds. Once done, it will continue with its own execution.
Expect                           Any → Any  Checks if the input value matches the expected type specified by the 'Type' parameter. The shard outputs the input value unchanged if it is of the appropriate type, the shard will trigger an error, preventing further execution. The 'Unsafe' parameter can be set to skip deep type hashing and comparison to improve performance.
ExpectAudio                      Any → Audio  Checks the input value if it is an Audio file. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectAudioSeq                   Any → Seq
ExpectBool                       Any → Bool  Checks the input value if it is a Boolean. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectBoolSeq                    Any → Seq  Checks if the input value is a sequence of Bools. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectBytes                      Any → Bytes  Checks the input value if it is of type Bytes. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectBytesSeq                   Any → Seq  Checks if the input value is a sequence of Bytes. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectColor                      Any → Color  Checks the input value if it is vector of four color channels (RGBA). The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectColorSeq                   Any → Seq  Checks if the input value is a sequence of Color vectors. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectFloat                      Any → Float  Checks the input value if it is of type Float. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, it will fail.
ExpectFloat2                     Any → Float2  Checks the input value if it is a vector with two Float elements. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectFloat2Seq                  Any → Seq  Checks if the input value is a sequence of Float2 vectors. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectFloat3                     Any → Float3  Checks the input value if it is a vector with three Float elements. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectFloat3Seq                  Any → Seq  Checks if the input value is a sequence of Float3 vectors. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectFloat4                     Any → Float4  Checks the input value if it is a vector with float Float elements. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectFloat4Seq                  Any → Seq  Checks if the input value is a sequence of Float4 vectors. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectFloatSeq                   Any → Seq  Checks if the input value is a sequence of Floats. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectImage                      Any → Image
ExpectImageSeq                   Any → Seq
ExpectInt                        Any → Int  Checks the input value if it is of type Int. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectInt16                      Any → Int16  Checks the input value if it is a vector with sixteen Int elements. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectInt16Seq                   Any → Seq  Checks if the input value is a sequence of Int16 vectors. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectInt2                       Any → Int2  Checks the input value if it is a vector with two Int elements. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectInt2Seq                    Any → Seq  Checks if the input value is a sequence of Int2 vectors. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectInt3                       Any → Int3  Checks the input value if it is a vector with three Int elements. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectInt3Seq                    Any → Seq  Checks if the input value is a sequence of Int3 vectors. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectInt4                       Any → Int4  Checks the input value if it is a vector with four Int elements. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectInt4Seq                    Any → Seq  Checks if the input value is a sequence of Int4 vectors. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectInt8                       Any → Int8  Checks the input value if it is a vector with eight Int elements. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectInt8Seq                    Any → Seq  Checks if the input value is a sequence of Int8 vectors. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectIntSeq                     Any → Seq  Checks if the input value is a sequence of Ints. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectLike                       Any → Any  Checks if the input value matches the type of the value provided in the TypeOf parameter or the output type of the given expression in the OutputOf parameter. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution. Note that it can only compare with either one of the parameters, not both; an error will be thrown if both are provided. The 'Unsafe' parameter can be set to skip deep type hashing and comparison to improve performance.
ExpectNone                       Any → None  Checks the input value if it is none. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectSeq                        Any → Seq  Checks if the input value is a sequence; otherwise, the shard will trigger an error, preventing further execution.
ExpectString                     Any → String  Checks the input value if it is a String. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectStringSeq                  Any → Seq  Checks if the input value is a sequence of Strings. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectTable                      Any → Table  Checks the input value if it is a Table. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectWire                       Any → Wire  Checks the input value if it is a Wire. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
ExpectWireSeq                    Any → Seq  Checks if the input value is a sequence of Wires. The shard outputs the input value unchanged if it is of the appropriate type; otherwise, the shard will trigger an error, preventing further execution.
Extend                           Seq → Seq  Extends the mutable sequence parameter with the elements of the input sequence.
FS.Absolute                      String → String
FS.Copy                          String → String
FS.CreateDirectories             String → String
FS.Extension                     String → String
FS.FileDialog                    None → String/Seq  Creates a file dialog to open files
FS.Filename                      String → String
FS.Is                            String → Bool  Returns true if two paths refer to the same file system object
FS.IsAbsolute                    String → Bool
FS.IsAny                         Seq → Bool  Returns true if the input sequence of paths contains a path equivalent to the given path
FS.IsDirectory                   String → Bool
FS.IsFile                        String → Bool
FS.IsNotAny                      Seq → Bool  Returns true if the input sequence of paths does not contain a path equivalent to the given path
FS.Iterate                       String → Seq
FS.Join                          Seq → String
FS.LastWriteTime                 String → Int
FS.Normalize                     String → String  Returns a normalized path by removing redundant separators and up-level references
FS.Parent                        String → String
FS.Read                          String → Bytes/String
FS.RelativeTo                    String → String
FS.Remove                        String → Bool
FS.RemoveAll                     String → Int
FS.Rename                        String → String
FS.ReplaceExtension              String → String
FS.SaveFileDialog                None → String
FS.SetWriteTime                  String → String
FS.Size                          String → Int
FS.Watch                         String → Seq/None  A directory watcher
FS.Write                         String → String
Fail                             String → None  Stops the current flow and cancels the execution with the provided error message. This shard is used to signal an error and halt the execution of the current wire.
FastInvSqrt                      Float/Float2/… → Float/Float2/…
FastSqrt                         Float/Float2/… → Float/Float2/…
FileOps.CreateFile               String → String
FileOps.InsertAtLine             String → String
FileOps.ReplaceStringAll         String → String
FileOps.ReplaceStringFirst       String → String
FileOps.ReplaceStringUnique      String → String
FileOps.ViewFile                 String → String
FillAlpha                        Image → Image  This shard takes an image with 3-channels(RGB) and converts it to a 4-channel(RGBA) image with the alpha channel set to 255.
Filter                           Seq → Seq
First                            Seq/Table → Any
Flatten                          Any → Any  This shard will take a sequence with nested values (eg. a sequence of sequences or a sequence of tables) and create a single sequence with all of values, nested values and keys as elements.
FloatsToAudio                    Seq → Audio
FloatsToImage                    Seq → Image  Converts a sequence of floats into an image. The image dimensions (width and height) and the number of channels are specified by the appropriate parameters.
Floor                            Float/Float2/… → Float/Float2/…  This shard rounds down the input to the nearest integer.
Flush                            Any → Any
FlushLog                         Any → Any  This shard flushes the log buffer to the console. This ensures that any pending log messages are immediately written to the console.
Fold                             Seq → Any
ForEach                          Seq/Table → Seq/Table  Processes every element or key-value pair of a sequence/table with the shards specified in the `Apply` parameter. Note that this shard is able to use the $0 and $1 internal variables, as well as $i for the current index.
ForRange                         Any → Any
FreeCamera                       Seq → Seq  Provides editor free camera controls
FromBase58                       String → Bytes  This shard decodes the base58 encoded string and outputs it as a decoded byte array.
FromBase64                       String → Bytes  Decodes a Base64 encoded string to its original byte representation.
FromBytes                        Bytes → Any  This shard takes a serialized binary representation of a value and convert it back to its original type.
FromJson                         String → Any  This shard parses a JSON string and outputs its contents as the appropriate type.
FromLEB128                       Bytes → Int  This shard decodes the LEB128 encoded byte array and outputs it as an integer (signed or unsigned depending on what was specified in the Signed parameter).
GFX.Buffer                       Any → Object  Creates a new graphics buffer
GFX.BuiltinFeature               None → Object  This shard creates the ready-made feature object of the feature specified in the ID parameter, for use in a rendering pass.
GFX.BuiltinMesh                  None → Object  This shard creates the ready-made mesh object of the shape defined in the Type parameter.
GFX.ClearQueue                   Object → None  Removes all drawable objects from the input drawable queue.
GFX.CopyPass                     None → Object  This shard creates a render pass object that is meant for transferring render data from one stage of the render pipeline to the next. It is also able to make changes to the render data specified in the Inputs parameter, like changing its texture format or down sampling the texture. It makes these changes through its Outputs and OutputScale parameters.
GFX.DisplayRefreshRate           None → Float
GFX.Draw                         Object/Seq → Object/Seq  This shard takes the input drawable object (or sequence of drawable objects) and adds them to the draw queue (created by GFX.DrawQueue) specified in the Queue parameter.
GFX.DrawQueue                    None → Object  Creates a new drawable queue object to add drawables to (using GFX.Draw).
GFX.Drawable                     Seq → Object  This shard creates a drawable object that can be added to a drawables queue for the render pipeline.
GFX.DrawablePass                 None → Object  This shard creates a render pass object, meant for rendering drawable objects, using the drawables from the drawables queue (specified in the Queue parameter) and the sequence of features objects (specified in the Features parameter).
GFX.DynDrawText                  String → None  Draws text to a dynamic mesh.
GFX.DynDrawTextPlacement         Seq → None  Draws text to a dynamic mesh.
GFX.DynDrawTextWorldSpace        String → None  Draws text to a dynamic mesh.
GFX.DynMesh                      None → Object  Creates a dynamic text mesh.
GFX.DynToMesh                    Object → Seq  Converts a dynamic text mesh into a static mesh
GFX.EffectPass                   None → Object  This shard creates a render pass object designed for applying post processing effects or full-screen rendering techniques.
GFX.EndFrame                     Any → Any  Explicitly end frame rendering, this is done automatically inside MainWindow. This shards is only needed when you want to end a frame earlier inside MainWindow
GFX.Feature                      None → Object  This shard creates a feature object based on what was provided in the different parameters.
GFX.FontMap                      Bytes → Object  Creates a font map from font data.
GFX.FontSpaceSize                Object → Int2  Retrieves the monospace character spacing size from a FontMap.
GFX.MainWindow                   None → Object
GFX.Material                     None → Object  This shard creates a material from the given parameters and features.
GFX.Mesh                         Table → Object  This shard creates a mesh object using the data provided in the Vertices and Indices keys of the input table.
GFX.MoveWindow                   Int2 → Int2  This shard moves the window specified in the Window parameter to the position specified in the input.
GFX.QueueDrawables               Object → Seq  Retrieves the drawable objects from the input drawable queue and outputs it as a sequence of drawable objects.
GFX.ReadBuffer                   Any → Bool  Creates a new graphics buffer
GFX.ReadTexture                  Object → Image  Adds a render step that reads back the rendered textures into a images, the returned images
GFX.Render                       None → None  This shard takes the sequence of render pass objects specified in the Steps parameter, processes it sequentially, and renders the final scene based on the view object specified in the View parameter.
GFX.RenderInto                   Any → Any  Renders within a region of the screen and/or to a render target
GFX.RenderTarget                 Any → Object  Groups a collection of textures into a render target that can be rendered into
GFX.RenderTargetTexture          Object → Object  Retrieve a named attachment from a render target
GFX.Renderer                     Any → Any
GFX.ResizeWindow                 Int2 → Int2  This shard resizes the window specified in the Window parameter to the dimensions specified in the input.
GFX.TextPlacement                String → Seq  Places text and returns the placement structure.
GFX.Texture                      Image/None/Any → Object  Creates a texture from an image. Or as a render target
GFX.UIPass                       None → Object  This shard creates a render pass object designed for rendering UI using the UI drawable objects from the drawables queue specified in the Queue parameter.
GFX.UIScaleFactor                None → Float  Outputs the scaling factor for UI as determined by the operating system.
GFX.View                         Any → Object  This shard creates and manages a view object, which defines and controls the view or camera settings for a rendering pipeline.
GFX.ViewMatrix                   Object → Seq  This shard takes the input view object and outputs its view matrix, which represents the camera's position and orientation in the world space.
GFX.ViewProjectionMatrix         Object → Seq  This shard takes the input view object and outputs its combined view-projection matrix, which encapsulates both the view position and orientation(view matrix) and its orthographic projection(projection matrix).
GFX.ViewRange                    Object → Float2  This shard takes the input view object and outputs a float2 representing the view's near and far clipping range. The first value in the float2 represents the near clipping range while the second value represents the far clipping range.
GFX.Viewport                     None → Int4  This shard outputs the dimensions of the current viewport as an int4
GFX.WindowFocused                None → Bool  This shard outputs whether the window specified in the Window parameter currently has focus.
GFX.WindowInsets                 None → Float4  Retrieves the window inset values when rendering on mobile devices and screens with a keep-out area.
GFX.WindowPosition               None → Int2  This shard outputs the position of the window specified in the Window parameter.
GFX.WindowSize                   None → Int2  This shard outputs the dimensions of the window specified in the Window parameter.
GFX.WriteBuffer                  Any → Any  Creates a new graphics buffer
GFX.glTF                         Seq/Table → Object  This shard takes the glTF model (specified either in the Path, Bytes or Copy parameter) and outputs a drawable object which can be subsequently added to the drawables queue for the render pipline.
GLTF.PackGLB                     String → Bytes  This shard takes the glTF file (specified by the input file path) and converts it into GLB format.
Geo.Buffer                       Table → Table
Geo.FilterVisible                Seq → Seq
Geo.GridFill                     Table → Seq
Geo.PathLength                   Seq → Float
Geo.Polygon                      Seq → Table
Geo.SimplifyPath                 Seq → Seq
Geo.ToDjiKmz                     Seq → Seq
Geo.ToGeoJSON                    Seq → Seq
Geo.ToGoogleEarth                Seq → Seq
Geo.ToLitchiCSV                  Seq → Seq
Get                              None → Any  Reads the value of the specified variable.
GetCounter                       Any → Int
GetFlag                          Any → Bool
GetImagePixel                    Int2 → Int4/Float4  This shard analyzes the image(specified in the Image parameter) and outputs the colour value(RGBA) of the pixel at the specified position.
Gizmos.Arrow                     Seq → None  Shows an arrow
Gizmos.Box                       Any → None  Draws a box in 3d space
Gizmos.Circle                    Any → None  Draws a line in 3d space
Gizmos.Context                   Any → Any  Provides a context for rendering gizmos
Gizmos.Debug                     Any → Any  Shows the renderer debug visuals
Gizmos.Disc                      Any → None  Draws a filled disc in 3d space
Gizmos.Grid                      Any → None  Draws a grid
Gizmos.Highlight                 Object/Seq → None  Queues a draw operation to highlight a drawable
Gizmos.Line                      Any → None  Draws a line in 3d space
Gizmos.Point                     Any → None  Draws a point in 3d space
Gizmos.Rect                      Any → None  Draws a rectangle in 3d space
Gizmos.RefspaceGridOverlay       Any → None  Draws a grid
Gizmos.Rotation                  Table/Seq → Table/Seq  Shows a rotation gizmo
Gizmos.Scaling                   Table/Seq → Table/Seq  Shows a scaling gizmo
Gizmos.ScreenScale               Float → Float  Outputs a scaling factor to give an object a uniform size on the screen
Gizmos.ScreenXY                  None → Seq  Outputs an X and Y direction that are aligned with the current view
Gizmos.SolidRect                 Any → None  Draws a filled rectangle in 3d space
Gizmos.Text                      Any → None  Draws text
Gizmos.Translation               Table/Seq → Table/Seq  Shows a translation gizmo
GlobalOnce                       Any → Any  Executes the shard or sequence of shards only once per mesh global execution.
Grep                             String → Seq/None
Hash                             Any → Int2  This shard takes any input type, hashes them using the XXH128 hashing algorithm and outputs their 128-bit hash value as an int2 (a sequence with 2 integers as elements).
Hash.Blake2-128                  Bytes/Seq/… → Bytes  This shard outputs the compact 128-bit hash(of 16 bytes) of the input (string, bytes or a sequence of strings/bytes) using the BLAKE2b-128 hashing algorithm.
Hash.Blake2-256                  Bytes/Seq/… → Bytes  This shard outputs the 256-bit hash(of 32 bytes) of the input (string, bytes or a sequence of strings/bytes) using the BLAKE2b-256 hashing algorithm.
Hash.Keccak-256                  Bytes/Seq/… → Bytes  This shard outputs the 256-bit hash (32-bytes) of the input (string, bytes or a sequence of strings/bytes) using the Keccak-256 hashing algorithm.
Hash.Keccak-512                  Bytes/Seq/… → Bytes  This shard outputs the 512-bit hash (64-bytes) of the input (string, bytes or a sequence of strings/bytes) using the Keccak-512 hashing algorithm.
Hash.Sha2-256                    Bytes/Seq/… → Bytes  This shard outputs the 256-bit hash (32-bytes) of the input (string, bytes or a sequence of strings/bytes) using the Keccak-256 hashing algorithm.
Hash.Sha2-512                    Bytes/Seq/… → Bytes  This shard outputs the 512-bit hash (64-bytes) of the input (string, bytes or a sequence of strings/bytes) using the SHA2-512 hashing algorithm.
Hash.Sha3-256                    Bytes/Seq/… → Bytes  This shard outputs the 256-bit hash (32-bytes) of the input (string, bytes or a sequence of strings/bytes) using the SHA3-256 hashing algorithm.
Hash.Sha3-512                    Bytes/Seq/… → Bytes  This shard outputs the 512-bit hash (64-bytes) of the input (string, bytes or a sequence of strings/bytes) using the SHA3-512 hashing algorithm.
Hash.XXH-128                     Bytes/Seq/… → Bytes  This shard outputs the 128-bit hash(of 16 bytes) of the input (string, bytes or a sequence of strings/bytes) using the XXH128 hashing algorithm.
Hash.XXH-64                      Bytes/Seq/… → Bytes  This shard outputs the 64-bit hash(of 8 bytes) of the input (string, bytes or a sequence of strings/bytes) using the XXH64 hashing algorithm.
Hash.XXH3-128                    Bytes/Seq/… → Bytes  This shard outputs the 128-bit hash(of 16 bytes) of the input (string, bytes or a sequence of strings/bytes) using the XXH3(128-bit) hashing algorithm.
Hash.XXH3-64                     Bytes/Seq/… → Bytes  This shard outputs the 64-bit hash(of 8 bytes) of the input (string, bytes or a sequence of strings/bytes) using the XXH3(64-bit) hashing algorithm.
HexToBytes                       String → Bytes  Converts a hexadecimal string to its original byte array representation.
Highest                          Seq → Any  Takes a sequence and outputs the element with the highest value.
Http.Chunk                       String/Bytes → String/Bytes
Http.Delete                      None/Table/… → Table/Bytes/…  This shard sends a HTTP DELETE request to the specified URL and outputs the response.
Http.Get                         None/Table → Table/Bytes/…  This shard sends a GET request to the specified URL and outputs the response.
Http.Head                        None/Table → Table/Bytes/…  This shard sends a HEAD request to the specified URL and outputs the response.
Http.Patch                       None/Table/… → Table/Bytes/…  This shard sends a HTTP PATCH request to the specified URL and outputs the response.
Http.Post                        None/Table/… → Table/Bytes/…  This shard sends a HTTP POST request to the specified URL and outputs the response.
Http.Put                         None/Table/… → Table/Bytes/…  This shard sends a HTTP PUT request to the specified URL and outputs the response.
Http.Read                        None → Table  This shard reads incoming HTTP requests from a client connection, parses its components, and outputs them as a table. This shard should be used in conjunction with the Http.Server shard to handle incoming requests. It properly handles both regular and chunked transfer encoding.
Http.Response                    String/Bytes → String/Bytes  This shard sends an HTTP response to the client after receiving an HTTP request.
Http.SendFile                    String → String  This shard sends a static file to the client over HTTP.
Http.Server                      Any → Any  This shard sets up an HTTP server that listens for incoming connections, creates new peers for each connection, and delegates request handling to the specified handler wire. It manages the lifecycle of connections and ensures proper cleanup when the server is stopped.
Http.Stream                      None → Bytes  Reads data from a previously opened stream.
If                               Any → Any  Evaluates a predicate and executes an action.
ImageSize                        Image → Int2  This shard takes an image and outputs its dimensions (width and height) as an Int2 vector.
ImageToBytes                     Image → Bytes  Converts an image into a byte array.
ImageToFloats                    Image → Seq  Convert an image into a sequence of floats. Each pixel in the image is converted to a float value between 0 and 1 and stored in the sequence.
Inc                              Any → Any  Increases the input by 1.
IncCounter                       Any → Int
IndexOf                          Seq → Seq/Int  This shard will search the input sequence for the index of an item or a pattern of items (specified in the Item parameter) and return its index(or a sequence of indices).
Input                            None → Any  The input value of the wire.
Inputs.DebugUI                   Any → Any  Shows the input system debug UI
Inputs.Detached                  Any → Any  Runs the contents on the input thread, and it's continuation on the current thread with the last data from the input thread
Inputs.GetContext                None → Object  This shard obtains the current window context and makes the information (window size, current time since application started, delta time since last frame, input events etc.) available to the rest of the Shards program.
Inputs.HandleURL                 None → Any  This shard listens to file drop events and URL events. When files are dropped onto the application or URLs events are received, this shard executes the specified Action for each file dropped or URL event received. The file path or URL is then passed as a string to the Action.
Inputs.IsKeyDown                 None → Bool  This shard outputs true if the key specified is currently pressed down, and false otherwise.
Inputs.KeyDown                   Any → Any  This shard checks if the key specified is pressed down. If the key is pressed down, the shard executes the code specified in the Action parameter on the frame the key is pressed down. If the Repeat parameter is set to true, the code specified in the Action parameter will be repeated every frame the key is held down instead.
Inputs.KeyUp                     Any → Any  This shard checks if the key specified is released. If the key is released, the shard executes the code specified in the Action parameter on the frame the key is released.
Inputs.MatchModifier             None → Bool  This shard outputs true if any of the modifier keys in the sequence provided in the Modifier parameter are currently pressed down, and false otherwise.
Inputs.MouseDelta                None → Float2  This shard outputs how much the mouse has moved since the last frame as a float2 vector. The first element represents the horizontal movement (a positive value indicates movement to the right), and the second element represents the vertical movement (a positive value indicates movement downwards).
Inputs.MouseDown                 Any → Any  Checks if the appropriate mouse button is pressed down. If it is pressed down, the shard executes the code specified in the respective parameter on the frame the button is pressed down. (If the Right Mouse button was pressed down, the code specified in the Right parameter will be executed.)
Inputs.MousePixelPos             None → Int2  This shard outputs the current pixel position of the cursor within the input region, represented as an int2 vector. The first element represents the x position of the cursor, and the second element represents the y position of the cursor. The coordinates are in pixel space, with (0,0) being the top-left corner of the input region and (input-region-pixel-width, input-region-pixel-height) being the bottom-right corner.
Inputs.MousePos                  None → Float2  This shard outputs the current logical position of the cursor within the input region, represented as a float2 vector. The first element represents the x position of the cursor, and the second element represents the y position of the cursor. The coordinates are in the same space as the input region's size, with (0,0) being the top-left corner and (input-region-width,input-region-height) being the bottom-right corner.
Inputs.MouseUp                   Any → Any  Checks if the appropriate mouse button is released. If it is released, the shard executes the code specified in the respective parameter on the frame the button is released.(If the Right Mouse button was released, the code specified in the Right parameter will be executed.)
Inputs.PixelSize                 None → Int2  This shard outputs the pixel size of the input region represented as an int2 vector. The first element represents the width of the region, and the second element represents the height of the region.
Inputs.Size                      None → Float2  This shard outputs the size of the input region represented as a float2 vector. The first element represents the width of the region, and the second element represents the height of the region.
Insert                           Any → Any  Prepends the input to the context variable passed to `Collection`.
IntRange                         None → Seq
IntsToBytes                      Seq → Bytes  Converts a sequence of integers into a byte array. Each integer in the sequence is serialized into its binary representation and concatenated into the resulting byte array.
Is                               Any → Bool  Checks if the input is equal to the operand.
IsAll                            Any → Bool  Checks if all elements in the input are equal to the given value. It outputs true if all elements are equal and false otherwise.
IsAllLess                        Any → Bool  Checks if all elements in the input are less than the given value. It outputs true if all elements are less and false otherwise.
IsAllLessEqual                   Any → Bool  Checks if all elements in the input are less than or equal to the given value. It outputs true if all elements are less or equal and false otherwise.
IsAllMore                        Any → Bool  Checks if all elements in the input are greater than the given value. It outputs true if all elements are greater and false otherwise.
IsAllMoreEqual                   Any → Bool  Checks if all elements in the input are greater than or equal to the given value. It outputs true if all elements are greater or equal and false otherwise.
IsAllNot                         Any → Bool  Checks if all elements in the input are not equal to the given value. It outputs true if all elements are not equal and false otherwise.
IsAlmost                         Float/Float2/… → Bool  Checks whether the input is almost equal to a given value.
IsAny                            Any → Bool  Checks if any element in the input is equal to the given value. It outputs true if any element is equal and false otherwise.
IsAnyLess                        Any → Bool  Checks if any element in the input is less than the given value. It outputs true if any element is less and false otherwise.
IsAnyLessEqual                   Any → Bool  Checks if any element in the input is less than or equal to the given value. It outputs true if any element is less or equal and false otherwise.
IsAnyMore                        Any → Bool  Checks if any element in the input is greater than the given value. It outputs true if any element is greater and false otherwise.
IsAnyMoreEqual                   Any → Bool  Checks if any element in the input is greater than or equal to the given value. It outputs true if any element is greater or equal and false otherwise.
IsAnyNot                         Any → Bool  Checks if any element in the input is not equal to the given value. It outputs true if any element is not equal and false otherwise.
IsAudio                          Any → Bool  Checks the input value if it is an Audio file. The shard will return true if the input is of the appropriate type, and false otherwise.
IsBool                           Any → Bool  Checks the input value if it is a Boolean. The shard will return true if the input is of the appropriate type, and false otherwise.
IsBytes                          Any → Bool  Checks the input value if it is of type Bytes. The shard will return true if the input is of the appropriate type, and false otherwise.
IsColor                          Any → Bool  Checks the input value if it is vector of four color channels (RGBA). The shard will return true if the input is of the appropriate type, and false otherwise.
IsFalse                          Bool → Bool  Gets whether the input is `false`.
IsFloat                          Any → Bool  Checks the input value if it is of type Float. The shard will return true if the input is of the appropriate type, and false otherwise.
IsFloat2                         Any → Bool  Checks the input value if it is a vector with two Float elements. The shard will return true if the input is of the appropriate type, and false otherwise.
IsFloat3                         Any → Bool  Checks the input value if it is a vector with three Float elements. The shard will return true if the input is of the appropriate type, and false otherwise.
IsFloat4                         Any → Bool  Checks the input value if it is a vector with float Float elements. The shard will return true if the input is of the appropriate type, and false otherwise.
IsImage                          Any → Bool
IsInt                            Any → Bool  Checks the input value if it is of type Int. The shard will return true if the input value is of type Int, and false otherwise.
IsInt16                          Any → Bool  Checks the input value if it is a vector of 16 Int elements. The shard will return true if the input is of the appropriate type, and false otherwise.
IsInt2                           Any → Bool  Checks the input value if it is a vector of 2 Int elements. The shard will return true if the input is of the appropriate type, and false otherwise.
IsInt3                           Any → Bool  Checks the input value if it is a vector of 3 Int elements. The shard will return true if the input is of the appropriate type, and false otherwise.
IsInt4                           Any → Bool  Checks the input value if it is a vector of 4 Int elements. The shard will return true if the input is of the appropriate type, and false otherwise.
IsInt8                           Any → Bool  Checks the input value if it is a vector of 8 Int elements. The shard will return true if the input is of the appropriate type, and false otherwise.
IsLess                           Any → Bool  Checks if the input is less than the operand.
IsLessEqual                      Any → Bool  Checks if the input is less than or equal to the operand.
IsMore                           Any → Bool  Checks if the input is greater than the operand.
IsMoreEqual                      Any → Bool  Checks if the input is greater than or equal to the operand.
IsNone                           Any → Bool  Gets whether the type of the input is `None`.
IsNot                            Any → Bool  Checks if the input is not equal to the operand.
IsNotNone                        Any → Bool  Gets whether the type of the input is different from `None`.
IsRunning                        None → Bool  Checks if a Wire is running and outputs true if it is, false if otherwise. (Note that a looped Wire will always be running and thus will always return true)
IsSeq                            Any → Bool  Checks the input value if it is of the type specified. The shard will return true if the input is of the appropriate type, and false otherwise.
IsString                         Any → Bool  Checks the input value if it is a String. The shard will return true if the input is of the appropriate type, and false otherwise.
IsTable                          Any → Bool  Checks the input value if it is a Table. The shard will return true if the input is of the appropriate type, and false otherwise.
IsTrue                           Bool → Bool  Gets whether the input is `true`.
IsValidNumber                    Float → Bool  Checks if the input is a valid floating-point number (not zero, subnormal, infinity, or NaN). Outputs true if the input is a normal floating-point number, otherwise outputs false.
IsWire                           Any → Bool  Checks the input value if it is a Wire. The shard will return true if the input is of the appropriate type, and false otherwise.
Isolate                          Any → Any  Isolates the inner shards' environment by only allowing certain variables
Iterate                          Table → Table
Jinja.Apply                      Table → String
Jwt.Decode                       String → String  Decodes a JWT token
Jwt.Encode                       String → String  Encodes a JSON payload into a JWT token
Jwt.Verify                       String → Bool  Verifies a JWT token signature without fully decoding it
Keys                             Table → Seq
LLM.AddBos                       Object → Object
LLM.AddImage                     Image → Image
LLM.AddText                      String → String
LLM.Bos                          Object → String
LLM.Chat                         Object → Object
LLM.Context                      Object → Object
LLM.Detokenize                   Seq → String
LLM.Embed                        Seq → Seq
LLM.Generate                     Object → String
LLM.Model                        String/Seq → Object
LLM.Reset                        Object → Object
LLM.Template                     Object → String
LLM.Tokenize                     String → Table/Seq
Last                             Seq/Table → Any
LastError                        None → String  This shard outputs the last error message that occurred as a string.
Lerp                             Float → Int/Int2/…  Linearly interpolate between the start value specified in the `First` parameter and the end value specified in the `Second` parameter based on the factor provided as input.
Limit                            Seq → Any  This shard truncates the input sequence to the specified number of elements(specified by the Max parameter) and outputs the truncated sequence.
Listen                           Any → Any
LoadImage                        Bytes/Any → Image  This shard loads an image from a file (specified in the File parameter) or byte array (passed as input) and outputs it as an image type, that can subsequently be used by other shards such as UI.Image and UI.ImageButton.
LocalShell.Create                None → Object
LocalShell.Execute               String → Table
LocalShell.IsAlive               Object → Bool
LocalShell.Read                  None → String
LocalShell.Resize                None → None
LocalShell.SendInput             String → Table
LocalShell.WaitFor               None → String
LocalShell.Write                 String → String
Log                              Any → Any
LogType                          Any → Any  Logs the type of the value to the console along with an optional prefix string. The logging level can be specified to control the verbosity of the log output.
Lowest                           Seq → Any  Takes a sequence and outputs the element with the lowest value.
ML.Detokenize                    Seq/Object → String  Converts token IDs or tensors back into text using a tokenizer.
ML.DetokenizeWithTimestamps      Seq/Object → String  Converts token IDs back into text segments with timestamps using a tokenizer.
ML.Forward                       Seq → Seq  Forward a tensor through a model.
ML.Model                         String → Object  This shard allows you to load a machine learning model and specify its format and configuration.
ML.PrepareAudioTokens            String → Seq/Object  Prepares the initial tokens for Whisper audio processing with language and task settings.
ML.Tokenizer                     String → Object  Loads a tokenizer from an input json string, ready to be used for tokenizing text.
ML.Tokens                        String → Seq  Tokenizes text using a tokenizer.
Map                              Seq/Table → Any
Markdown.FromHTML                String → String  Converts HTML to Markdown
Markdown.Parse                   String → Seq  A markdown commonmark pull parser.
Match                            Any → Any
Math.Abs                         Int/Int2/… → Int/Int2/…  This shard outputs the absolute value of the input.
Math.Acos                        Float/Float2/… → Float/Float2/…
Math.Acosh                       Float/Float2/… → Float/Float2/…
Math.Add                         Int/Int2/… → Int/Int2/…  This shard adds the input value to the value provided in the Operand parameter.
Math.And                         Int/Int2/… → Int/Int2/…
Math.Asin                        Float/Float2/… → Float/Float2/…
Math.Asinh                       Float/Float2/… → Float/Float2/…
Math.Atan                        Float/Float2/… → Float/Float2/…
Math.Atan2                       Int/Int2/… → Int/Int2/…
Math.Atanh                       Float/Float2/… → Float/Float2/…
Math.AxisAngleX                  Float → Float4  This shard creates a rotation quaternion for rotation around the X-axis. It takes a float input representing the angle in radians and outputs the rotation quaternion as a float4 vector. A float4 vector is a vector with 4 float elements.
Math.AxisAngleY                  Float → Float4  This shard creates a rotation quaternion for rotation around the Y-axis. It takes a float input representing the angle in radians and outputs the rotation quaternion as a float4 vector. A float4 vector is a vector with 4 float elements.
Math.AxisAngleZ                  Float → Float4  This shard creates a rotation quaternion for rotation around the Z-axis. It takes a float input representing the angle in radians and outputs the rotation quaternion as a float4 vector. A float4 vector is a vector with 4 float elements.
Math.Cbrt                        Float/Float2/… → Float/Float2/…  This shard calculates the cube root of the given input.
Math.Ceil                        Float/Float2/… → Float/Float2/…  This shard rounds up the input to the nearest integer.
Math.Compose                     Table → Seq  Creates a 4x4 transformation matrix (sequence of four float4 vectors) from a table containing the appropriate Translation, Rotation and Scale values. values. The translation value should be a float3 vector representing positions on the x y z axis. The rotation value should be a float4 vector representing the quaternion rotation. Lastly, the scale should be a float3 vector representing the size on the x y and z axis. Eg. {translation: @f3(1 2 3), rotation: @f4(0 0 0 1), scale: @f3(1 1 1)} A float3 vector is a vector with 3 float elements while a float4 vector is a vector with 4 float elements.
Math.Conjugate                   Float4 → Float4
Math.Cos                         Float/Float2/… → Float/Float2/…  This shard calculates the cosine of the given input, where the input is the angle in radians.
Math.Cosh                        Float/Float2/… → Float/Float2/…
Math.Cross                       Float3/Seq → Float3/Seq  This shard computes the cross product of the float3 vector (or sequence of float3 vectors) provided as input and the float3 vector provided in the Operand parameter and outputs the result as a float3 vector (or sequence of float3 vectors). A float3 vector is a vector with 3 float elements.
Math.Dec                         Any → Any  Decreases the input by 1.
Math.Decompose                   Seq → Table  This shard converts a 4x4 transformation matrix (a sequence of four float 4 vectors) into a table containing its constituent Translation, Rotation, and Scale components. The table has a Translation key with a float3 vector value representing positions on the x, y, z axes, a Rotation key with a float4 vector value representing the quaternion rotation, and a Scale key with a float3 vector value, representing the size on the x, y, z axes. Eg. {translation: @f3(1 2 3), rotation: @f4(0 0 0 1), scale: @f3(1 1 1)} A float3 vector is a vector with 3 float elements while a float4 vector is a vector with 4 float elements.
Math.DegreesToRadians            Float → Float  This shard converts the input angle from degrees to radians. The conversion is done using the formula: radians = degrees * (π / 180).
Math.Divide                      Int/Int2/… → Int/Int2/…  This shard divides the input value by the value provided in the Operand parameter.
Math.Dot                         Float2/Seq/… → Float2/Seq/…  Computes the dot product of two float vectors with an equal number of elements, and outputs the resulting float value. The first float vector is passed as input and the second float vector is specified in the Operand parameter.
Math.Erf                         Float/Float2/… → Float/Float2/…
Math.Erfc                        Float/Float2/… → Float/Float2/…
Math.EulerToQuat                 Float3 → Float4
Math.Exp                         Float/Float2/… → Float/Float2/…
Math.Exp2                        Float/Float2/… → Float/Float2/…
Math.Expm1                       Float/Float2/… → Float/Float2/…  This shard calculates the exponential function with base e (Euler's number) for the given input and subtracts 1 from the result.
Math.FastInvSqrt                 Float/Float2/… → Float/Float2/…
Math.FastSqrt                    Float/Float2/… → Float/Float2/…
Math.Floor                       Float/Float2/… → Float/Float2/…  This shard rounds down the input to the nearest integer.
Math.Inc                         Any → Any  Increases the input by 1.
Math.Inverse                     Seq → Seq  This shard takes a 4x4 matrices as input and computes its inverse. A 4x4 matrix is a sequence with exactly 4 float4 vectors while a float4 vector is a vector with 4 float elements.
Math.LGamma                      Float/Float2/… → Float/Float2/…
Math.LShift                      Int/Int2/… → Int/Int2/…
Math.Length                      Float2/Seq/… → Float  Computes the magnitude of a float vector of any dimension and outputs the result as a float.
Math.LengthSquared               Float2/Seq/… → Float  Computes the squared magnitude of a float vector of any dimension and outputs the result as a float.
Math.Lerp                        Float → Int/Int2/…  Linearly interpolate between the start value specified in the `First` parameter and the end value specified in the `Second` parameter based on the factor provided as input.
Math.Log                         Float/Float2/… → Float/Float2/…
Math.Log10                       Float/Float2/… → Float/Float2/…
Math.Log1p                       Float/Float2/… → Float/Float2/…  This shard adds 1 to the input and then calculates the natural logarithm of the result.
Math.Log2                        Float/Float2/… → Float/Float2/…
Math.LookAt                      Table → Seq  This shard creates a 4x4 view matrix (a sequence of four float4 vectors) for a camera based on the camera's position and a target point which is represented as a table with two float3 vectors: 'Position' and 'Target', that is passed as input. A float4 vector is a vector with 4 float elements while a float3 vector is a vector with 3 float elements.
Math.MatIdentity                 None → Seq  This shard creates a standard 4x4 identity matrix. The standard identity matrix is a square matrix with 1s on the main diagonal and 0s for the other elements. A 4x4 matrix is a sequence with exactly 4 float4 vector and a float4 vector is a vector with 4 float elements.
Math.MatMul                      Seq → Seq/Float4/…  Performs matrix multiplication on either two matrices or a matrix and a vector and outputs either a matrix or a vector accordingly. The two matrixes must be of similar dimensions (2x2, 3x3, or 4x4). And if multiplied with a vector, the vector too must have similar dimensions (2x2 with float2, 3x3 with float3, 4x4 with float4).
Math.Mean                        Seq → Float  Calculates the average value of a sequence of floating point numbers.
Math.Mod                         Int/Int2/… → Int/Int2/…  This shard calculates the remainder of the division of the input value by the value provided in the Operand parameter.
Math.Multiply                    Int/Int2/… → Int/Int2/…  This shard multiplies the input value by the value provided in the Operand parameter.
Math.MultiplyAdd                 Float/Float2/… → Float/Float2/…
Math.Negate                      Int/Int2/… → Int/Int2/…  This shard reverses the sign of the input. (A positive number becomes negative, and vice versa).
Math.Normalize                   Seq/Float2/… → Seq/Float2/…  This shard normalizes a float vector of any dimension or a sequence of floats, scaling it to have a magnitude of 1 while preserving its direction. By default, output values can range from -1.0 to 1.0. If the 'Positive' parameter is set to true, the output will be scaled to the range 0.0 to 1.0. For example, normalizing [4.0 -5.0 6.0 -7.0] will result in [0.3563, -0.4454, 0.5345, -0.6236], which has a length of 1.
Math.Not                         Int/Int2/… → Int/Int2/…
Math.Or                          Int/Int2/… → Int/Int2/…
Math.Orthographic                None → Seq  This shard creates a 4x4 orthographic projection matrix based on the width size, height size, near, and far planes specified in the appropriate parameters. A 4x4 matrix is a sequence with exactly 4 float4 vectors while a float4 vector is a vector with 4 float elements.
Math.Percentile                  Seq → Float  This shard calculates the percentile of the input value within the specified sequence.
Math.Pow                         Int/Int2/… → Int/Int2/…  This shard raises the input to the power of the exponent specified in the Operand parameter.
Math.Project                     Float3 → Float3  This shard converts the input 3D world coordinates to 2D screen coordinates using a view-projection matrix. Both 3D and 2D coordinates are represented as float3 vectors (vectors with 3 float elements).It performs the full projection pipeline including matrix multiplication, perspective division, and viewport transformation using the 4x4 view-projection matrix specified in the Matrix parameter and the screen size in the ScreenSize parameter.
Math.QuatMultiply                Float4 → Float4  This shard multiplies two quaternions (represented as float4 vectors) together. It combines the two rotations by multiplying the input quaternion with the operand quaternion. A float4 vector is a vector with 4 float elements.
Math.QuatRotate                  Float3 → Float3  This shard rotates the input 3D vector (represented as a float3) by the quaternion (represented as a float4) specified in the Operand parameter and outputs the resulting rotated 3D vector. A float4 vector is a vector with 4 float elements while a float3 vector is a vector with 3 float elements.
Math.RShift                      Int/Int2/… → Int/Int2/…
Math.RadiansToDegrees            Float → Float  This shard converts the input angle from radians to degrees. The conversion is done using the formula: degrees = radians * (180 / π).
Math.Rotation                    Float4 → Seq  This shard creates a 4x4 rotation matrix (a sequence of four float4 vectors) from a float4 vector input representing a rotation quaternion. A float4 vector is a vector with 4 float elements.
Math.Round                       Float/Float2/… → Float/Float2/…
Math.Scaling                     Float3 → Seq  This shard creates a 4x4 scaling matrix (a sequence of four float4 vectors) from a float3 vector input that represents the scaling factors in x, y, and z directions. A float4 vector is a vector with 4 float elements while a float3 vector is a vector with 3 float elements.
Math.Sin                         Float/Float2/… → Float/Float2/…  This shard calculates the sine of the given input, where the input is the angle in radians.
Math.Sinh                        Float/Float2/… → Float/Float2/…
Math.Slerp                       Float → Float4  This shard performs Spherical Linear Interpolation (SLERP) between two quaternions (represented as float4 vectors). It smoothly interpolates between the quaternions specified in the 'First' parameter and 'Second' parameter based on the input interpolation factor. A float4 vector is a vector with 4 float elements.
Math.Sqrt                        Float/Float2/… → Float/Float2/…  This shard calculates the square root of the given input.
Math.Subtract                    Int/Int2/… → Int/Int2/…  This shard subtracts the value provided in the Operand parameter from the input value.
Math.TGamma                      Float/Float2/… → Float/Float2/…  This shard calculates the gamma function of the given input.
Math.Tan                         Float/Float2/… → Float/Float2/…  This shard calculates the tangent of the given input, where the input is the angle in radians.
Math.Tanh                        Float/Float2/… → Float/Float2/…
Math.Translation                 Float3 → Seq  This shard creates a 4x4 translation matrix (a sequence of four float4 vectors) from a float3 vector input representing the translation in x, y, and z directions. A float4 vector is a vector with 4 float elements while a float3 vector is a vector with 3 float elements.
Math.Transpose                   Seq → Seq  Performs matrix transposition on the input matrix. Transposition flips the matrix over its main diagonal, switching its rows and columns. This shard supports 2x2, 3x3, and 4x4 as input matrices. A 4x4 matrix is a sequence with exactly 4 float4 vectors, a 3x3 matrix is a sequence with exactly 3 float3 vectors, and a 2x2 matrix is a sequence with exactly 2 float2 vectors.
Math.Trunc                       Float/Float2/… → Float/Float2/…
Math.Unproject                   Float3 → Float3  This shard converts 2D screen coordinates back to 3D world coordinates using the inverse of a view-projection matrix. Both 3D and 2D coordinates are represented as float3 vectors (vectors with 3 float elements).It performs the reverse operation of the projection pipeline, including inverse matrix multiplication, and coordinate space transformations using the 4x4 view-projection matrix specified in the Matrix parameter and the screen size in the ScreenSize parameter.
Math.Xor                         Int/Int2/… → Int/Int2/…
Max                              Int/Int2/… → Int/Int2/…  This shard compares the input with the value specified in the `Operand` parameter and outputs the larger value.
Maybe                            Any → Any  Attempts to activate a shard or a sequence of shards. Upon failure, activate another shard or sequence of shards.
Memoize                          Any → Any  Computes a value
Merge                            Table → Table  Combine two tables into one, with the input table taking priority over the operand table, which will be written and returned as output. This shard is useful in scenarios where you need to merge data from different sources while keeping the priority of certain values.
Min                              Int/Int2/… → Int/Int2/…  This shard compares the input with the value specified in the `Operand` parameter and outputs the smaller value.
Mnemonic.Generate                Int → String  Generates a BIP39 mnemonic
Mnemonic.ToSeed                  String → Bytes  Converts a BIP39 mnemonic to a seed
Msg                              Any → Any  Displays the passed message string to the user via standard output. The input variable is ignored, and only the static message is displayed.
Mul                              Int/Int2/… → Int/Int2/…  This shard multiplies the input value by the value provided in the Operand parameter.
MulAdd                           Float/Float2/… → Float/Float2/…
NaNTo0                           Float/Seq → Float/Seq  Replaces NaN (Not a Number) values in the input with 0. This shard can handle both single float values and sequences of float values.
NanoID                           None → String  Creates a random NanoID.
Network.Broadcast                Any → Any  This shard sends the input to all peers connected to the server (created by Network.Server) specified in the Server parameter.
Network.Client                   Any → Object  This shard creates a UDP client connection using the KCP protocol, on the address and port specified in the Address and Port parameters.
Network.Disconnect               Object → Object
Network.Peer                     Any → Object  This shard outputs the peer object of the peer with the Peer ID specified in the Peer parameter.
Network.PeerID                   Any → Int  This shard outputs the Peer ID of the peer specified in the Peer parameter as an integer.
Network.STUN                     Any → Table
Network.Send                     Any → Any  This shard sends the input to the peer specified in the Peer parameter.
Network.SendRaw                  String/Bytes → Any  This shard sends the input byte array or string to the peer specified in the Peer parameter.
Network.Server                   Any → Object  This shard sets up a UDP network server using the KCP protocol, on the address and port specified in the Address and Port parameters. This server then handles client connections and disconnections, messages received from clients and broadcasting messages to these clients.
Network.WS.Client                Any → Object  This shard creates a WebSocket client connection using the TCP, on the address specified in the Address.
Network.WS.Server                Any → Object  This shard sets up a Websocket network server using TCP, on the address and port specified in the Address and Port parameters. This server then handles client connections and disconnections, messages received from clients and broadcasting messages to these clients.
Not                              Bool → Bool  Computes the logical negation of the input.
Once                             Any → Any  Executes the shard or sequence of shards with the desired frequency in a wire flow execution.
Or                               Bool → Bool  Computes the logical OR between the input of this shard and the output of the next shard. If the input is true, the flow stops and succeeds; if false, the flow continues with the next shard. Typically used within conditional flows (e.g., If, When) to chain conditions. Note: Outside a conditional flow, it might restart the current wire, which can be used as a trick in certain scenarios.
PDF.ToText                       Bytes → String  Extract text from a stream of bytes representing a PDF document
Pack                             Seq → Bytes
Pad                              Audio/Seq/… → Any
ParseFloat                       String → Float  Converts the string representation of a number to its floating-point number equivalent.
ParseInt                         String → Int  Converts the string representation of a number to its signed integer equivalent.
Pass                             Any → Any  This shard is a "no operation" shard. It simply passes through the input without modifying it.
Pause                            Any → Any  Pauses the wire for a given amount of time.
PauseMs                          Any → Any  Pauses the wire for a given amount of time.
Peek                             None → Any  Checks if another Wire has ended (Note that a looped Wire will never end). Outputs the Wire's output if it has ended, or none if it is still in progress.
Percentile                       Seq → Float  This shard calculates the percentile of the input value within the specified sequence.
Physics.AngularVelocity          Object → Float3  Retrieves the angular velocity of the input physics body
Physics.ApplyForce               Object → Object  This shard applies a short instantaneous linear and/or angular force to the input physics body.
Physics.ApplyForceAt             Object → Object  Applies a force to the physics body at a specific location
Physics.ApplyImpulse             Object → Object  This shard applies a continuous linear and/or angular force to the input physics body.
Physics.Body                     Any → Object  This shard creates a physics body, conducts physics simulations on this body while updating the relavent variables tied to this body, and creates an interface to allow other physics shards to interact with this body.
Physics.BoxShape                 Float3 → Object  This shard creates a box collision shape from the input half extents provided.
Physics.CapsuleShape             Any → Object  This shard creates a capsule physics collision shape, using the height and radius provided in the HalfHeight and Radius parameters respectively. The capsule will be centered around the origin with one sphere cap at (0, -HalfHeight, 0) and the other at (0, HalfHeight, 0).
Physics.CenterOfMass             Object → Float3  Retrieves the position of the center of mass of the input physics body
Physics.Collisions               Object → Seq  Gets the list of current contacts for the input physics body and runs the shards specified when a new contact is detected or when a contact is removed.
Physics.Context                  Any → Object  Creates and initializes the core physics context object that will manage the physics simulation environment.
Physics.DebugDraw                Any → Any
Physics.DistanceConstraint       Any → Any  This shard creates a tether between two bodies, keeping the distance between them within the range specified in the MinDistance and MaxDistance parameters.
Physics.Dump                     Any → Any  Logs information about the current state of the physics simulation in the console.
Physics.End                      Any → Any  This shard runs the physics simulation. It should be called after defining physics bodies to be included in the simulation.
Physics.FixedConstraint          Any → Any  This shard rigidly connects two physics bodies together. When forces are applied to either body, they react as if they were a single rigid object.
Physics.HullShape                Object → Object  Creates a physics shape from a GFX mesh or drawable input
Physics.InverseMass              Object → Float  Retrieves the inverse mass of the input physics body. The inverse mass is 1.0 divided by the mass of the input physics body.
Physics.LinearVelocity           Object → Float3  Retrieves the linear velocity of the input physics body
Physics.Location                 Object → Float3  Retrieves the position of the physics body
Physics.MotionType               Object → Enum  Retrieves the motion type of the input physics body
Physics.Rotation                 Object → Float4  Retrieves the rotation of the physics body
Physics.SetPose                  Object → Object  Overwrites the position and/or rotation of the input physics body.
Physics.SetVelocity              Object → Object  Overwrites the linear and/or angular velocity of the input physics body
Physics.SliderConstraint         Any → Any  This shard creates a slider constraint between two bodies, turning the two bodies into sliders that can slide along the axis specified in the SliderAxis parameter.
Physics.SoftBody                 Any → Object  Defines a new node
Physics.SoftBodyShape            Object → Object
Physics.SphereShape              Float → Object  Create a sphere collision shape, from the input radius provided.
Physics.WithContext              Any → Any
Pop                              None → Any  Pops (removes and outputs) the last element of the sequence variable. Works only on sequences. If the variable is not a sequence or the sequence is empty, an error is thrown.
PopFront                         None → Any  Pops (removes and outputs) the first element of the sequence variable. Works only on sequences. If the variable is not a sequence or the sequence is empty, an error is thrown.
Pow                              Int/Int2/… → Int/Int2/…  This shard raises the input to the power of the exponent specified in the Operand parameter.
PremultiplyAlpha                 Image → Image  Applies the premultiplication of alpha channels of an image to its RGB channels. Does nothing if the image has already been premultiplied in Shards. This mainly applies to PNG images.
PrependTo                        Any → Any  Prepends the input to the context variable passed to `Collection`.
Process.Exe                      None → String  Gives the current executable path.
Process.Run                      String → String
Process.Shell                    String → String
Process.StackTrace               None → String
Process.StdIn                    None → String  Reads a line from standard input.
Process.StdOut                   String/Bytes → String/Bytes  Writes a string to standard output.
Produce                          Any → Any
Profile                          Any → Any  This shard outputs the amount of time it took to execute the shards provided in the Action parameter, automatically choosing the most appropriate time unit (ns, μs, ms, s).
PtrToString!                     Int → String
Push                             Any → Any  Pushes a new value into a sequence variable. If the variable does not exist, it will be created.
Py                               Any → Any
Py.Eval                          Any → Any
RLimit                           Seq → Any  This shard truncates the input sequence to the last specified number of elements (Max) and outputs the truncated sequence. If Max is set to 1, it outputs a single element.
RTake                            Bytes/String/Seq → Any  Works exactly like `Take` except that the selection indices are counted backwards from the last element in the target sequence. Also, `RTake` works only on sequences, not on tables.
RadiansToDegrees                 Float → Float  This shard converts the input angle from radians to degrees. The conversion is done using the formula: degrees = radians * (180 / π).
Random.Name                      None → String  Generate a random name (Petname)
RandomBytes                      None → Bytes  This shard generates a random sequence of bytes. The size of the sequence is specified in the Size parameter.
RandomFloat                      None → Float  This shard generates a random float between 0 and the maximum value specified in the Max parameter (exclusive).
RandomInt                        None → Int  This shard generates a random integer between 0 (inclusive) and the maximum value specified in the Max parameter (exclusive).
ReadFile                         None → Any
Reduce                           Seq → Any
Ref                              Any → Any  Creates an immutable reference variable. Once created this variable cannot be changed.
Reflect.Get                      None → Any  Gets a variable dynamically by name. Returns None if the variable doesn't exist.
Reflect.Name                     ShardRef → String
Reflect.Shards                   Wire → Seq  Given a wire as input it will recurse deep inside it and gather all shards in the wire, its sub-wires and sub-flows.
Regex.Match                      String → Seq  This shard matches the entire input string against the regex pattern specified in the Regex parameter and outputs a sequence of strings, containing the fully matched string and any capture groups. It will return an empty sequence if there are no matches.
Regex.Replace                    String → String  This shard modifies the input string by replacing all occurrences of the regex pattern, specified in the Regex parameter, with the replacement string specified in the Replacement parameter.
Regex.Search                     String → Seq  This shard searches the input string for the regex pattern specified in the Regex parameter and outputs a sequence of strings, containing every occurrence of the pattern. An empty sequence is returned if there are no matches
Remove                           None → Seq  Removes all elements from a sequence that match the given condition. Can also take these matched indices and remove corresponding elements from a joined sequence.
Repeat                           Any → Any  Repeat an action a given number of times or until the 'Until' parameter returns true.
Replace                          Seq/String → Seq/String  This shard replaces all occurrences of the pattern(specified in the Patterns parameter) found in the input sequence or string, with replacements (specified in the Replacements parameter).
ResizeImage                      Image → Image  This shard resizes an image to the specified dimensions specified in the Width and Height parameters.
Restart                          Any → None  Restarts the current flow with the provided input. This shard is used to restart the execution of the current wire from the beginning, using the same input. It ensures that the input type matches the wire's root input type. Note: This is a flow stopper and will not continue to subsequent shards in the current execution sequence.
Resume                           Any → Any  Resumes another Wire (previously paused using Suspend).
Return                           Any → None
Reverse                          Seq/String/Bytes → Seq/String/Bytes  This shard reverses the order of the elements in the input sequence or string.
Run                              Any → Bool
SSH.Connect                      None → Object
SSH.Execute                      String → Table
SSH.IsConnected                  Object → Bool
SSH.Read                         None → String
SSH.Resize                       None → None
SSH.SendInput                    String → Table
SSH.WaitFor                      None → String
SSH.Write                        String → String
SVG.ToImage                      String/Bytes → Image  Converts an SVG string or bytes to an image.
Schedule                         Any → Any
Sequence                         Any → Any  Creates an empty sequence (or sequence in a table if a key is passed). Useful to declare and specify types.
Set                              Any → Any  Creates a mutable variable and assigns a value to it.
SetCounter                       Int → Int
SetFlag                          Bool → Bool
SetLogLevel                      String → Any  This shard changes the log level to the level specified by the string passed as input.
Shader.LinearizeDepth            Float → Float  This shard converts non-linear depth buffer values to linear depth value.
Shader.Literal                   None → Any/None  This shard allows the user to write WGSL code directly and insert it into the shader code. The WGSL code is written as a sequence of strings in the Source parameter.
Shader.ReadBuffer                None → Any  This shard reads the shader parameter (specified in the Name parameter) from the buffer (specified in the Buffer Name parameter).
Shader.ReadGlobal                None → Any  This shard reads the value of the global shader variable specified in the Name parameter.
Shader.ReadInput                 None → Any  This shard reads the value of the shader input specified in the Name parameter.
Shader.RefBuffer                 Any → Object  Outputs a reference to the default sampler object for a named texture.
Shader.RefSampler                Any → Object  Outputs a reference to the default sampler object for a named texture.
Shader.RefTexture                Any → Object  Outputs a reference to the texture object for a named texture.
Shader.SampleTexture             None → Float4  Samples a named texture with default texture coordinates
Shader.SampleTextureCoord        Float4/Float3/… → Float4  Samples a named texture with the passed in texture coordinates
Shader.WithInput                 None → None  This shard creates a conditional statement within a shader code. If the shader input specified in the Name parameter is available to the shader stage that calls this shard, the code in the Then parameter will be executed. Otherwise, the code in the Else parameter will execute.
Shader.WithTexture               None → None  This shard creates a conditional statement within a shader code. If the texture specified in the Name parameter is available for the vertex or pixel, the code in the Then parameter will be executed. Otherwise, the code in the Else parameter will execute.
Shader.WriteGlobal               Any → None  This shard sets the value passed as input to the global shader variable specified in the Name parameter.
Shader.WriteOutput               Any → None  This shard writes the input value to the shader output or one of the outputs of the render pass (specified in the Name parameter).
Shards.Distill                   String/Bytes/Object → Wire
Shards.Docs                      String → String
Shards.EnumTypeHelp              Int → Table
Shards.EnumTypes                 None → Seq
Shards.Enumerate                 None → Seq
Shards.Errors                    Object → Seq  Fetches all errors from a live AST Object input
Shards.Help                      String → Table
Shards.ObjectTypeHelp            Int → Table
Shards.ObjectTypes               None → Seq
Shards.Print                     String/Bytes/Object → String  Pretty prints shards AST into a string.
Shards.Read                      String → String/Bytes/…  Reads the textual representation of a Shards program and outputs the binary or json AST representation.
Shuffle                          Any → Any  Shuffles the elements of the sequence variable. Works only on sequences. If the variable is not a sequence, it simply passes through without failing.
Sin                              Float/Float2/… → Float/Float2/…  This shard calculates the sine of the given input, where the input is the angle in radians.
SleepBlocking!                   Any → Any
Slice                            Seq/Bytes/… → Any  Extracts characters from a string or elements from a sequence based on the start and end positions/indices and an increment parameter. Operation is non-destructive; the target string/sequence is not modified.
Snappy.Compress                  Bytes → Bytes  This shard compresses the input byte array using the Snappy algorithm and outputs the compressed byte array.
Snappy.Decompress                Bytes → Bytes  This shard decompressed the input byte array that has been previously compressed using the Snappy.Compress shard.
Snowflake                        None → Int  Creates a Snowflake ID.
Sort                             None → Seq  Sorts the elements of a sequence. Can also move around the elements of a joined sequence in alignment with the sorted sequence.
Spawn                            Any → Wire  Schedules the specified Wire and runs it asynchronously. The current Wire will continue its execution independently of the specified Wire. Unlike Detach, a copy of the specified Wire is scheduled every time the shard is called.
Split                            Seq/Bytes/String → Seq
Spreadsheet.Load                 String/Bytes → Object
Spreadsheet.Read                 Object → Seq/Table
Spreadsheet.ReadAll              String/Bytes/Object → Table
Spreadsheet.Sheets               Object → Seq
Sqrt                             Float/Float2/… → Float/Float2/…  This shard calculates the square root of the given input.
Sr25519.PublicKey                Bytes → Bytes  Extracts the public key from a Sr25519 keypair
Sr25519.Sign                     Bytes → Bytes  Signs a message using the Schnorr signature on Ristretto compressed Ed25519 points.
Sr25519.Verify                   Bytes → Bool  Verifies a Sr25519 signature
Step                             Any → Any  The first time Step is called, the specified wire is scheduled. On subsequent calls, the specified Wire's state is progressed before the current Wire continues its execution. This means that a pause in execution of the child Wire will not pause the parent Wire.
Stop                             Any → Any  Either stops the execution of a specified Wire or the current Wire.
String.CleanUTF8                 String → String
String.CodePoints                String → Seq  Converts a string into a sequence of integer codepoints.
String.Contains                  String → Bool  This shard checks if the input string contains the string specified in the String parameter. If the input string does contain the string specified, the shard will output true. Otherwise, it will output false.
String.DecodeURI                 String → String  This shard decodes a URI-encoded string back into its original format.
String.EncodeURI                 String → String  This shard encodes a string into a URI-encoded format making it safe to use in URLs.
String.Ends                      String → Bool  This shard checks if the input string ends with the string specified in the With parameter. If the input string does contain the string specified, the shard will output true. Otherwise, it will output false.
String.Find                      String → Int  Finds the next occurence of the string specified in the String parameter in the input string and outputs the index of the first occurence.
String.Format                    Seq → String  This shard concatenates all the elements of a sequence into a string
String.FromCodePoints            Seq → String  Converts a sequence of integer codepoints into a string.
String.FromUTF16                 Bytes → String
String.Join                      Seq → String  This shard concatenates all the elements of a string sequence, using the specified separator between each element.
String.RFind                     String → Int  Finds the last occurence of the string specified in the String parameter in the input string and outputs the index of the first occurence.
String.Split                     String → Seq  This shard splits the input string into a sequence of its constituent strings, using the string specified in the Separator parameter to segment the input. If the KeepSeparator parameter is true, the separator will be included in the output.
String.Starts                    String → Bool  This shard checks if the input string starts with the string specified in the With parameter. If the input string does contain the string specified, the shard will output true. Otherwise, it will output false.
String.ToLower                   String → String  This shard converts all characters in the input string to lowercase.
String.ToUTF16                   String → Bytes
String.ToUpper                   String → String  This shard converts all characters in the input string to uppercase.
String.Trim                      String → String  This shard removes all leading and trailing whitespace characters from the input string and outputs the trimmed string.
StringToBytes                    String → Bytes  Converts a string to its byte representation.
StripAlpha                       Image → Image  This shard takes an image with 4 channels(RGBA) and converts it to a 3-channel(RGB) image with the alpha channel removed.
Sub                              Int/Int2/… → Int/Int2/…  This shard subtracts the value provided in the Operand parameter from the input value.
Suspend                          Any → Any  Pauses a specified Wire's execution. If no Wire is specified, pauses the current wire.
Swap                             Any → Any  Swaps the values of the two variables passed to it via `First` and `Second` parameters.
SwitchTo                         Any → Any  Suspends the current Wire and switches execution to the specified Wire.
Table                            Any → Any  Creates an empty table. Useful to declare and specify types.
Take                             Int2/Int3/… → Any  Extracts one or more elements from a sequence or values from a table using the provided indices or keys. This operation is non-destructive and does not modify the target sequence or table. If the key cannot be established to exist at compose time, the output will be of type Any.
Tan                              Float/Float2/… → Float/Float2/…  This shard calculates the tangent of the given input, where the input is the angle in radians.
TargetCamera                     Table → Table  Provides editor free camera controls
TargetCamera.FromLookAt          None → Table  Provides editor free camera controls
TargetCamera.Matrix              Table → Seq  Turns the target camera state into a view matrix
Tensor                           Seq → Object  Creates a tensor from a sequence (or nested sequences) of variables.
Tensor.Add                       Object → Object  Adds two tensors element-wise.
Tensor.Div                       Object → Object  Divides two tensors element-wise.
Tensor.MatMul                    Object → Object  Performs matrix multiplication of two tensors.
Tensor.Mul                       Object → Object  Multiplies two tensors element-wise.
Tensor.Pow                       Object → Object  Raises each element of the tensor to the power of the corresponding element in another tensor.
Tensor.Reshape                   Object → Object  Reshapes the tensor to the specified shape.
Tensor.Shape                     Object → Seq  Outputs the shape of the tensor.
Tensor.Slice                     Object → Object  Slices a tensor along a specified dimension.
Tensor.Split                     Object → Seq  Splits a tensor into multiple tensors along a specified dimension.
Tensor.Stack                     Seq → Object  Stacks a sequence of tensors along a new axis.
Tensor.Sub                       Object → Object  Subtracts two tensors element-wise.
Tensor.Sum                       Object → Object  Computes the sum of tensor elements along the specified dimensions.
Tensor.ToFloat                   Object → Float  Converts a single-element tensor to a float (f64) scalar.
Tensor.ToFloat2s                 Object → Seq
Tensor.ToFloat3s                 Object → Seq
Tensor.ToFloat4s                 Object → Seq
Tensor.ToFloats                  Object → Seq  Flattens a tensor into a Shards sequence of floats.
Tensor.ToInts                    Object → Seq  Flattens a tensor into a Shards sequence of integers.
Tensor.ToString                  Object → String  Outputs a string representation of a tensor.
Tensor.Transpose                 Object → Object  Transposes the dimensions of the tensor.
Tensor.UMAP                      Object → Object
Tensor.ZerosLike                 Object → Object  Creates a tensor of zeros with the same shape as the input tensor.
Time.Delta                       None → Float  Outputs the time between the last call of this shard and the current call in seconds, capped to a limit
Time.DeltaMs                     None → Float  Outputs the time between the last call of this shard and the current call in milliseconds, capped to a limit
Time.Epoch                       None → Int  This shard outputs the the amount of time that has elapsed from the Unix epoch to the current system time in seconds.
Time.EpochLocal                  None → Int  This shard outputs the amount of time that has elapsed from the Unix epoch to the current local system time in seconds.
Time.EpochLocalMs                None → Int  This shard outputs the amount of time that has elapsed from the Unix epoch to the current local system time in milliseconds.
Time.EpochMs                     None → Int  This shard outputs the the amount of time that has elapsed from the Unix epoch to the current system time in milliseconds.
Time.MovingAverage               Float → Float  This shard computes the average of a floating point number over a specified number of frames.
Time.Now                         None → Float
Time.NowMs                       None → Float
Time.ToString                    Int/Float → String  This shard converts time into a human readable string.
ToAny                            Any → Any  Converts an integer, bytes, or string value into its hexadecimal string representation.
ToAnySeq                         Seq → Seq
ToAnyTable                       Table → Table
ToBase58                         Bytes/String → String  This shard encodes the input(either a byte array or a string) into a base58 encoded string.
ToBase64                         Bytes/String → String  Encodes the input bytes or string value to its Base64 string representation.
ToBytes                          Any → Bytes  This shard takes a value and converts it to a serialized binary representation (a serialized byte array).
ToColor                          Any → Any  Converts various input types to a vector of four color channels (RGBA). If a single value or a collection with less than 4 elements is provided, the remaining unaccounted elements in the resulting vector will be set to 0.
ToFloat                          Any → Any  Converts various input types to type Float.
ToFloat2                         Any → Any  Converts various input types to a vector of two Float elements. If a single value or a collection with only one element is provided, the second element in the resulting vector will be set to 0.
ToFloat3                         Any → Any  Converts various input types to a vector of three Float elements. If a single value or a collection with less than 3 elements is provided, the remaining unaccounted elements in the resulting vector will be set to 0.
ToFloat4                         Any → Any  Converts various input types to a vector of Four Float elements. If a single value or a collection with less than 4 elements is provided, the remaining unaccounted elements in the resulting vector will be set to 0.
ToHex                            Int/Int16/… → String  Converts an integer, bytes, or string value into its hexadecimal string representation.
ToInt                            Any → Any  Converts various input types to type Int.
ToInt16                          Any → Any  Converts various input types to a vector of sixteen Int elements. If a single value or a collection with less than 16 elements is provided, the remaining unaccounted elements in the resulting vector will be set to 0.
ToInt2                           Any → Any  Converts various input types to a vector of two Int elements. If a single value or a collection with only one element is provided, the second element in the resulting vector will be set to 0.
ToInt3                           Any → Any  Converts various input types to a vector of three Int elements. If a single value or a collection with less than 3 elements is provided, the remaining unaccounted elements in the resulting vector will be set to 0.
ToInt4                           Any → Any  Converts various input types to a vector of four Int elements. If a single value or a collection with less than 4 elements is provided, the remaining unaccounted elements in the resulting vector will be set to 0.
ToInt8                           Any → Any  Converts various input types to a vector of eight Int elements. If a single value or a collection with less than 8 elements is provided, the remaining unaccounted elements in the resulting vector will be set to 0.
ToJson                           Any → String  This shard takes its input and converts it into a JSON string.
ToLEB128                         Int → Bytes  This shard encodes the input(a signed or unsigned integer) into a LEB128 encoded byte array.
ToString                         Any → String  Converts any input value to its string representation.
Track                            Any → Any
Tracy.Message                    String → String
TraitId                          None → Int2  Retrieves the hash id of the given trait
Trigger                          Any → Any
Trim                             Audio/Seq/… → Any
TryMany                          Seq → Seq  This shard takes a sequence of values as input, schedules multiple copies of a specified Wire and executes them asynchronously. Each value from the sequence is provided as input to its corresponding copy of the scheduled Wire. The shard will then wait for all the scheduled Wires to end, and then, depending on the Policy specified, the shard will either return the output of the first successful Wire, return a sequence with all the output from all the copies of the specified Wire or stop execution of the current Wire if all the copies failed.
TypeOf                           None → Type  Evaluates the output type of the given expression specified by the 'OutputOf' parameter and outputs that type. No input is required for this shard.
UI                               Any → Object  Initializes a UI context
UI.AddFonts                      Seq → Seq  Adds custom fonts to the UI system, the input should a seq of a pair (name, bytes).
UI.Area                          Any → Any  Places UI element at a specific position.
UI.AutoGrid                      Seq → Seq  Works like UI.Grid, but given a Sequence, it will, it each object in the Sequence, execute the Shard provided in its Contents and automatically wrap the generated contents when it exceeds the grid's width.
UI.BottomPanel                   Any → Any  Layout UI elements into the panel.
UI.Button                        Any → Bool  Clickable button with text.
UI.Canvas                        Any → Any  A canvas to draw UI elements on
UI.CentralPanel                  Any → Any  Layout UI elements into the central panel.
UI.Checkbox                      None → Bool  Boolean on/off widget with text label.
UI.CloseMenu                     Any → Any  Closes the currently opened menu.
UI.CodeEditor                    None → Bool  A TextField with support for highlighting.
UI.Collapsing                    Any → Any  A header which can be collapsed/expanded, revealing a contained UI region.
UI.ColorInput                    None → Color  A widget where a color can be selected.
UI.Columns                       Any → Any  Splits the contents into several columns.
UI.Combo                         Any → Any  A drop-down selection menu with a label.
UI.Console                       String → String  A console with formatted logs.
UI.CountInputEvents              Object → Int
UI.Disable                       Any → Any  Creates a scoped child UI.
UI.DockArea                      Any → Object
UI.DragAndDrop                   Any → Any  Drag and drop source & target.
UI.Float2Input                   None → Float2  A numeric input.
UI.Float2Slider                  None → Float2  A numeric slider.
UI.Float3Input                   None → Float3  A numeric input.
UI.Float3Slider                  None → Float3  A numeric slider.
UI.Float4Input                   None → Float4  A numeric input.
UI.Float4Slider                  None → Float4  A numeric slider.
UI.FloatInput                    None → Float  A numeric input.
UI.FloatSlider                   None → Float  A numeric slider.
UI.Frame                         Any → Any  Visually groups the contents together.
UI.GetClipboard                  None → String  This shard retrieves the current system clipboard contents.
UI.Grid                          Any → Any  Simple grid layout.
UI.Group                         Any → Any  Visually groups the contents together.
UI.HexViewer                     Bytes/Enum/… → Bytes/Enum/…  Displays an hexadecimal viewer of data.
UI.Horizontal                    Any → Any  Layout the contents horizontally.
UI.Hyperlink                     String → String  A clickable hyperlink.
UI.Image                         Image/Object → Image/Object  Display an image in the UI.
UI.ImageButton                   Image/Object → Bool  Clickable button with image.
UI.Indent                        Any → Any  Creates a child UI which is indented to the right.
UI.Inspection                    Any → Any  Show inspection ui
UI.Int2Input                     None → Int2  A numeric input.
UI.Int2Slider                    None → Int2  A numeric slider.
UI.Int3Input                     None → Int3  A numeric input.
UI.Int3Slider                    None → Int3  A numeric slider.
UI.Int4Input                     None → Int4  A numeric input.
UI.Int4Slider                    None → Int4  A numeric slider.
UI.IntInput                      None → Int  A numeric input.
UI.IntSlider                     None → Int  A numeric slider.
UI.Label                         String → String  Static text.
UI.Layout                        Any → Any  Versatile layout with numerous customization options.
UI.LayoutClass                   Any → Object  This shard creates a Layout class that can be used in other UI shards.
UI.LeftPanel                     Any → Any  Layout UI elements into the panel.
UI.Link                          Any → Bool  A clickable link.
UI.ListBox                       Any → Any  A list selection.
UI.MarkdownViewer                String → String  A markdown viewer.
UI.Menu                          Any → Bool  Creates a menu button that when clicked will show the given menu. If called from within a menu this will instead create a button for a sub-menu.
UI.MenuBar                       Any → Bool  The menu bar goes well in a `UI.TopPanel`.
UI.NextRow                       Any → Any  Moves to the next row in a grid layout.
UI.Overlay                       Any → Any  Overlays multiple UI's on top of each other.
UI.PaintCircle                   Any → Any  Draw a circle on the canvas
UI.Plot                          Any → Any  A 2D plot area.
UI.PlotBar                       Seq → Seq  Points represented as vertical or horizontal bars on a plot.
UI.PlotLine                      Seq → Seq  Line-connected points on a plot.
UI.PlotPoints                    Seq → Seq  Scattered points on a plot.
UI.PopupWrapper                  Any → Any  Wraps a button with a popup that can act as a drop-down menu or suggestion menu.
UI.Positioned                    Float4 → Any
UI.ProgressBar                   Float → Float  A progress bar with an optional overlay text.
UI.Property                      None → Float4/Float2/…  Retrieves values from the current state of the UI.
UI.RadioButton                   None → Bool  A radio button for selecting a value amongst multiple choices.
UI.Render                        Seq/Object → Seq/Object  Render given UI
UI.RenderTarget                  Object → Object  Display the contents of a render target. Consumes input on the region.
UI.Reset                         Any → Any  Forgets UI scroll, position, size changes, etc.
UI.RestoreDockState              Bytes → Bytes
UI.RestoreState                  Bytes → Bytes  This shard restores the UI to a previously saved state (provided as input as a byte array).
UI.RightPanel                    Any → Any  Layout UI elements into the panel.
UI.SaveDockState                 Any → Bytes
UI.SaveState                     None → Bytes  This shard saves the current state of the UI as a byte array. It saves UI information such as the position of windows, the state of checkboxes, the state of sliders, etc.
UI.Scope                         Any → Any  Creates a scoped child UI.
UI.ScrollArea                    Any → Any  Add scrolling to contained UI elements.
UI.Selectable                    Any → Bool  A wrapper that detects selection over the provided contents.
UI.Separator                     Any → Any  A visual separator. A horizontal or vertical line (depending on the layout).
UI.SetClipboard                  String → String  This shard sets the input string to the system clipboard.
UI.SetCursor                     Float2 → Float2
UI.Settings                      Any → Any  Show settings ui
UI.Shards                        None → Bool  A Shards program AST visual editor.
UI.Sized                         Any → Any
UI.Space                         Any → Any  Inserts an empty space before the next widget. The direction will depend on the layout.
UI.Spinner                       Any → Any  A spinner widget used to indicate loading.
UI.Style                         Any → Any  Apply style changes to the current UI scope.
UI.SubArea                       Any → Any  Places UI element at a specific position.
UI.TabClose                      Any → Any
UI.TabIsOpen                     Any → Bool
UI.TabOpen                       Any → Any
UI.Table                         Int/Seq → Int/Seq  Table layout.
UI.TableHeader                   Any → Any  Defines a header for a Table column.
UI.TextField                     None → Any  A widget where text can be entered.
UI.Tooltip                       Any → Any  Display a tooltip when the Contents is hovered over.
UI.TopPanel                      Any → Any  Layout UI elements into the panel.
UI.Variable                      Any → Any  This shard creates a UI element that displays and allows interaction with the variable specified in the Variable parameter.
UI.Vertical                      Any → Any  Layout the contents vertically.
UI.WidgetStyle                   Any → Any  Apply style changes to the current UI scope.
UI.Window                        Any → Any  Creates a floating window which can be dragged, closed, collapsed, and resized.
UUID                             None → Int16  Outputs a UUID (Universally Unique Identifier).
UUID.Convert                     Bytes/String → Int16  Outputs a UUID (Universally Unique Identifier) as Int16.
UUID.ToBytes                     Int16 → Bytes
UUID.ToString                    Int16 → String
Unpack                           Int/Bytes → Seq
UnsafeActivate!                  Any → Any
Update                           Any → Any  Modifies the value of an existing mutable variable.
Values                           Table → Seq
VarAddr!                         String → Int  Retrieves the memory address of the specified variable and outputs it as an integer.
VarPtr!                          Any → Int  Retrieves the memory address of the data contained in the specified variable and outputs it as an integer. The variable must be of type sequence, bytes, or string.
Wait                             None → Any  Waits for the specified Wire to complete before resuming execution of the current Wire.
When                             Any → Any  Conditional shard that only executes the action if the predicate is true.
WhenDone                         Any → Any  Schedules the specified Wire and runs it asynchronously. The current Wire will continue its execution independently of the specified Wire. Unlike Detach, a copy of the specified Wire is scheduled every time the shard is called.
WhenNot                          Any → Any  Conditional shard that only executes the action if the predicate is false.
Whisper.Load                     String → Object
Whisper.Transcribe               Seq → String
WireComposer                     Wire → String  Attempts to compose the specified wire and outputs "OK" if successful, or an error message if the composition fails.
WireRunner                       Any → Any
WriteFile                        Any → Any
WriteJPG                         Image → Bytes/Image  Writes an image to a JPEG file or returns JPEG bytes.
WritePNG                         Image → Bytes/Image
X509.PublicKey                   String/Bytes → String  Extracts a public key from an X509 certificate and outputs it as a PEM string
X509.Verify                      Seq → Bool  Verifies a certificate chain against a root certificate
Yaml.FromJson                    String → String  A shard that converts JSON to YAML.
Yaml.ToJson                      String → String  A shard that converts YAML to JSON.
Zip                              None → Seq  Zip will take any number of sequences and return a sequence of sequences, where each sequence is a tuple of the values from the input sequences at the same index.
_Callgrind.Start                 Any → Any
_Callgrind.Stop                  Any → Any
_ExportStrings                   None → Seq
_Inputs.DiscardTempFiles         Any → Any  Discard temporary files created by open & drag-drop operations
_MakeColor                       None → Any  Creates a vector of four color channels (RGBA). If fewer than four values are provided, the remaining elements will be set to 0. The alias for this shard is @color.
_MakeFloat2                      None → Any  Creates a vector of two Float elements from the values provided in the parameters. If fewer than two values are provided, the remaining elements will be set to 0. The alias for this shard is @f2.
_MakeFloat3                      None → Any  Creates a vector of three Float elements from the values provided in the parameters. If fewer than three values are provided, the remaining elements will be set to 0. The alias for this shard is @f3.
_MakeFloat4                      None → Any  Creates a vector of four Float elements from the values provided in the parameters. If fewer than four values are provided, the remaining elements will be set to 0. The alias for this shard is @f4.
_MakeInt16                       None → Any  Creates a vector of sixteen Int elements from the values provided in the parameters. If fewer than sixteen values are provided, the remaining elements will be set to 0. The alias for this shard is @i16.
_MakeInt2                        None → Any  Creates a vector of two Int elements from the values provided in the parameters. If fewer than two values are provided, the remaining elements will be set to 0. The alias for this shard is @i2.
_MakeInt3                        None → Any  Creates a vector of three Int elements from the values provided in the parameters. If fewer than three values are provided, the remaining elements will be set to 0.The alias for this shard is @i3.
_MakeInt4                        None → Any  Creates a vector of four Int elements from the values provided in the parameters. If fewer than four values are provided, the remaining elements will be set to 0. The alias for this shard is @i4.
_MakeInt8                        None → Any  Creates a vector of eight Int elements from the values provided in the parameters. If fewer than eight values are provided, the remaining elements will be set to 0. The alias for this shard is @i8
_MakeTrait                       None → Any  This shard creates a trait with the specified name and types and makes it available for use in the wire.
_ProcessorCount                  Any → Int
_SubFlow                         Any → Any  Activates a shard or a sequence of shards independently, without consuming the input. I.e. the input of the Sub flow will also be its output regardless of the shards activated in this Sub flow.
```
