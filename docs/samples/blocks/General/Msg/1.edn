(defblocks msgblock [a b]
  (Msg a)  ;; print value of 1st arg passed
  (Msg b)) ;; print value of 2nd arg passed

(defloop main-chain
    (Msg "Hello World") ;; prints string
    (msgblock "Bye" "Universe") ;; prints args passed
)

(defnode root)
(schedule root main-chain)
(run root 1 1)
