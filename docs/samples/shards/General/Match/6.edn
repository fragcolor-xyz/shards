;; No match + no nil case + passthrough
false
(Match
 :Cases
 [true (-> (Msg "Not a match")) ;; no match found
  true (-> (Msg "Not a match either"))] ;; no match found
            ;; reached end of cases, no matches, no nil case => no shards activated
 :Passthrough
 true) ;; input allowed to passthrough to Match shard's output
(Log) ;; print Match shard's output => "false"
