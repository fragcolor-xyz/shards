;; ForEach on a sequence: processes every element in seq order
[2 4 8 10]
(ForEach
 (->
  (| (Math.Multiply 10)
     (Log))  ;; => 20, 40, 80, 100
  (| (Math.Multiply 100)
     (Log))  ;; => 200, 400, 800, 1000
  ))
