@wire(ui-wire {
  UI.CentralPanel(
    Contents:
    UI.Collapsing(
      Heading: UI.Button("Heading" Msg("Clicked"))
      DefaultOpen: true
      Contents: {"Content" (UI.Label)}
    )
  )
} Looped: true)

{ui-behavior: ui-wire}