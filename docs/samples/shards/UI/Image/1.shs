GFX.MainWindow(
  Contents: {
    Once({
      GFX.DrawQueue >= ui-draw-queue
      GFX.UIPass(ui-draw-queue) >> render-steps
    })
    UI(
      ui-draw-queue
      UI.CentralPanel({
        Once({
          LoadImage("data/ShardsLogo.png") = image
        })
        image | UI.Image(Scale: @f2(0.25))
      })
    )

    GFX.Render(Steps: render-steps)
  }
)