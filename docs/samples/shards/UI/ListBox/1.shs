GFX.MainWindow(
  Contents: {
    Once({
      GFX.DrawQueue >= ui-draw-queue
      GFX.UIPass(ui-draw-queue) >> render-steps
    })
    UI(
      ui-draw-queue
      UI.CentralPanel({
        ["α Α" "ω Ω"] | UI.ListBox(Index: index)
        | ExpectString >= value

        UI.Horizontal({
          "Selected index: " | UI.Label
          index | ToString | UI.Label
        })
      })
    )

    GFX.Render(Steps: render-steps)
  }
)