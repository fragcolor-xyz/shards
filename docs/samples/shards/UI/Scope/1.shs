@wire(ui-wire {
  UI.Window(
    Position: @f2(0 0)
    Anchor: Anchor::Center
    Width: 700
    Height: 400
    Contents: {
      UI.CentralPanel(
    UI.Scope(
     Contents:
     {"Hello" | UI.Label}))
    }
  )
} Looped: true)

{ui-behavior: ui-wire}