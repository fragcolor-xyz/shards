@wire(ui-wire {
  UI.CentralPanel(
    {["α Α" "ω Ω"]
      UI.Combo(
        Label: "Greek letter"
        Index: index
      )
      ExpectString >= letter
    }
  )
} Looped: true)

{ui-behavior: ui-wire}