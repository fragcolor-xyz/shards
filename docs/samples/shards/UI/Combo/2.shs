@wire(ui-wire {
  UI.CentralPanel(
    {["α Α" "ω Ω"]
      UI.Combo(
        Label: "Greek letter"
        Style: {color: @color(200 10 200)}
        Index: index
      )
      ExpectString >= letter
    }
  )
} Looped: true)

{ui-behavior: ui-wire}