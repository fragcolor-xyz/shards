GFX.MainWindow(
  Contents: {
    Once({
      GFX.DrawQueue >= ui-draw-queue
      GFX.UIPass(ui-draw-queue) >> render-steps
    })
    UI(
      ui-draw-queue
      UI.CentralPanel({
        UI.Checkbox(
          Label: "Checked"
          Variable: checked
        )
      })
    )

    GFX.Render(Steps: render-steps)
  }
)