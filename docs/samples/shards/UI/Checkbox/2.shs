@wire(ui-wire {
  UI.CentralPanel(
    UI.Checkbox(
      Label: "Checked"
      Style: {color: @color(255 0 255) italics: true}
      Variable: checked
    )
  )
} Looped:  true)

{ui-behavior: ui-wire}