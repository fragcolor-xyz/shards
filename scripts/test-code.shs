

@wire(main-wire{
  Once({
    [ 0 1 2 3 4 5] >= sequence

    sequence
    Replace(
      Patterns: [1]
      Replacements: [2]
    )
    Log("result")
  })
}Looped: true)

@mesh(main)
@schedule(main main-wire)
@run(main)