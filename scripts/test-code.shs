
@wire(main-wire{
  Once({
    
    [72 101 108 108 111]
    ;ToBytes -- serializes
    IntsToBytes
    BytesToString
    Log("result")
  })
}Looped: false)

@mesh(main)
@schedule(main main-wire)
@run(main)