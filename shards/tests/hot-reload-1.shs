@template(log-tmpl [msg] {
  Log(msg Name: "log-tmpl")
})

@wire(zero {
  none | Do(shared-wire)
  @log-tmpl("Hello 000")
})

@wire(one {
  Once({
    Log("Helo 1 once triggered")
  })
  @log-tmpl("Hello 111")
})

@wire(two {
  @log-tmpl("Hello 222")
})

{zero: zero one: one}
