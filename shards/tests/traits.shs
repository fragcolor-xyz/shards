@trait(toggleable {
  enabled: @type(Type::Bool)
})

@trait(card-owner {
  cards: @type({
    name: Type::String
    flags: @type([Type::String])
  })
})

@wire(my-wire {
  Once({
    false >= enabled
  })
} [toggleable card-owner])