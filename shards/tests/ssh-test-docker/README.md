# SSH Test Server

Simple SSH server for testing the SSH shards module.

## Usage

Start the server:
```bash
cd shards/tests/ssh-test-docker
docker-compose up -d
```

Stop the server:
```bash
docker-compose down
```

## Connection Details

- **Host**: localhost
- **Port**: 2222
- **Username**: testuser
- **Password**: test123
