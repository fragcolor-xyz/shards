@wire(main {
  TUI.VBox({
    TUI.HBox({
      "Hello" | TUI.Text
      TUI.Separator
      "World" | TUI.Text
    })
  }) ; | TUI.Render | Process.StdOut
  TUI.Tick
} Looped: true)

@mesh(root)
@schedule(root main)
@run(root FPS: 10)
