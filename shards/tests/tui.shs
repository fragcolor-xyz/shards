@wire(main {  
  Once({
    RandomInt | ToString = text
  } Every: 2.0)
  
  text | Memoize(TUI.Paragraph(TopTitle: "Hello" BottomTitle: "World")) >> elements
  
  elements | TUI.Draw
} Looped: true)

@mesh(root)
@schedule(root main)
@run(root FPS: 10
; Iterations: 10
)
