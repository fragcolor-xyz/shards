@interface(toggleable {
  enabled: @type(Type::Bool)
})

@interface(card-owner {
  cards: @type({
    name: Type::String
    flags: @type([Type::String])
  })
})

@wire(my-wire {
  Once({
    false >= enabled
  })
} [toggleable card-owner])